<?xml version="1.0" encoding="utf-8"?>
<search>
  
  
    
    <entry>
      <title><![CDATA[《Android APP可能有的东西》之UI篇：流式标签&搜索历史]]></title>
      <url>http://blog.prozin.xyz/2016/10/14/%E6%B5%81%E5%BC%8F%E6%A0%87%E7%AD%BE/</url>
      <content type="html"><![CDATA[<h2 id="说在前面"><a href="#说在前面" class="headerlink" title="说在前面"></a>说在前面</h2><p>很多APP都有热门排行热门搜索什么的，这个时候可能需要用到标签，也就是流式布局，而还有可能用到的就是搜索历史记录，这种一般都是使用listview展示。但是，相当有可能是反过来的UI展示效果，热门排行用listview,历史记录使用流式标签。所以这里给出相结合的Demo实例以及说明。</p>
<a id="more"></a>
<h3 id="上代码！FlowLayout-SearchHistory"><a href="#上代码！FlowLayout-SearchHistory" class="headerlink" title="上代码！FlowLayout-SearchHistory"></a>上代码！<a href="https://github.com/paradoxie/FlowLayout-SearchHistory/tree/master" target="_blank" rel="external">FlowLayout-SearchHistory</a></h3><h2 id="FlowLayout流式标签"><a href="#FlowLayout流式标签" class="headerlink" title="FlowLayout流式标签"></a>FlowLayout流式标签</h2><p>使用了继承至ViewGroup的自定义控件，数据载入方式为String数组。此自定义控件可以直接使用。</p>
<figure class="highlight java"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div><div class="line">17</div><div class="line">18</div></pre></td><td class="code"><pre><div class="line"></div><div class="line"><span class="function"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title">initData</span><span class="params">()</span> </span>&#123;</div><div class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; datas.length; i++) &#123;</div><div class="line">            TextView tv = (TextView) mInflater.inflate(</div><div class="line">                    R.layout.search_label_tv, mFlowLayout, <span class="keyword">false</span>);</div><div class="line">            tv.setText(datas[i]);</div><div class="line">            <span class="keyword">final</span> String str = tv.getText().toString();</div><div class="line">            <span class="comment">//点击事件</span></div><div class="line">            tv.setOnClickListener(<span class="keyword">new</span> View.OnClickListener() &#123;</div><div class="line">                <span class="meta">@Override</span></div><div class="line">                <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">onClick</span><span class="params">(View v)</span> </span>&#123;</div><div class="line">                    <span class="comment">//加入搜索历史纪录记录</span></div><div class="line">                    Toast.makeText(MainActivity.<span class="keyword">this</span>, str, Toast.LENGTH_LONG).show();</div><div class="line">                &#125;</div><div class="line">            &#125;);</div><div class="line">            mFlowLayout.addView(tv);</div><div class="line">        &#125;</div><div class="line">    &#125;</div></pre></td></tr></table></figure>
<p>标签的展示样式可以通过Demo里面的search_label_tv布局去自定义修改，这里的逻辑并不麻烦，详细代码查看demo代码。</p>
<p><strong>插播一个效果图：</strong></p>
<p><img src="http://odpd0x6u7.bkt.clouddn.com/FlowLayout.jpeg" alt="插播一个效果图"></p>
<h2 id="SearchHistory搜索历史"><a href="#SearchHistory搜索历史" class="headerlink" title="SearchHistory搜索历史"></a>SearchHistory搜索历史</h2><p>使用SharedPreferences记录搜索历史。</p>
<p>主要的方法如下：</p>
<pre><code>public void initSearchHistory() {}//初始化

public void save() {}//储存

public void cleanHistory() {}//清除
</code></pre><p>Demo里面为了代码逻辑的清晰，将历史记录的控件都与流式标签分离了，通常是放在一起的。另外，SharedPreferences也可以单独封装方法提供直接调用，只要能储存，怎么利索怎么写。</p>
<p>好像就这么点东西，没啥可以写的。。。</p>
<h2 id="最后"><a href="#最后" class="headerlink" title="最后"></a>最后</h2><p>Demo里面的代码量本身也不大，主要是供新手朋友们参考，也算是自己记录一下这些可以单独抽离出来的小东西。</p>
<h3 id="上代码！FlowLayout-SearchHistory-1"><a href="#上代码！FlowLayout-SearchHistory-1" class="headerlink" title="上代码！FlowLayout-SearchHistory"></a>上代码！<a href="https://github.com/paradoxie/FlowLayout-SearchHistory/tree/master" target="_blank" rel="external">FlowLayout-SearchHistory</a></h3><h1 id="以上"><a href="#以上" class="headerlink" title="以上"></a>以上</h1><hr>
<blockquote>
<p>本文作者：paradoxie</p>
<p>个人主页：<a href="http://www.paradoxie.cf/" target="_blank" rel="external">谢盒盒的小黑屋</a></p>
<p>简书地址：<a href="http://www.jianshu.com/users/05f39939cbf3/latest_articles" target="_blank" rel="external">简书主页</a></p>
<p>github地址：<a href="https://github.com/paradoxie" target="_blank" rel="external">paradoxie</a></p>
<p>转载请注明出处，蟹蟹!</p>
<p>——-我的梦想真的是做一条咸鱼！</p>
</blockquote>
]]></content>
    </entry>
    
    <entry>
      <title><![CDATA[《Android APP该有的东西》之功能篇：版本检测更新]]></title>
      <url>http://blog.prozin.xyz/2016/10/13/%E7%89%88%E6%9C%AC%E6%A3%80%E6%B5%8B%E6%9B%B4%E6%96%B0/</url>
      <content type="html"><![CDATA[<p>自从友盟的自动更新下线之后，一大批app选择了自己写更新逻辑。是的，这里的逻辑并不复杂，完全可以自己写，但是不复杂不代表不麻烦，像是否强制更新、是否能开启循环检测等功能，跟后台配合起来就显得麻烦了。</p>
<a id="more"></a>
<iframe frameborder="no" border="0" marginwidth="0" marginheight="0" width="330" height="86" src="http://music.163.com/outchain/player?type=2&id=34376248&auto=0&height=66"></iframe>

<p>后来发现了腾讯的Bugly，他是这么介绍的：升级功能是专为App的灰度升级而开发的组件，在bugly内测页面配置好App的更新策略，策略指定的老版本App在启动时会自动检测更新并提示升级，为团队的应用分发，灰度内测提供一站式解决方案。</p>
<p>本文就自己写更新逻辑和集成腾讯bugly自动更新记录一下说明和代码，如果有朋友恰好需要，又恰好看到了，又恰好用上了，又恰好解决了问题，算是功德一件咯~</p>
<h4 id="上代码！AutoUpdateDemo"><a href="#上代码！AutoUpdateDemo" class="headerlink" title="上代码！AutoUpdateDemo"></a>上代码！<a href="https://github.com/paradoxie/AutoUpdateDemo" target="_blank" rel="external">AutoUpdateDemo</a></h4><h2 id="首先是跟后台配合的更新"><a href="#首先是跟后台配合的更新" class="headerlink" title="首先是跟后台配合的更新"></a>首先是跟后台配合的更新</h2><p><strong>讲思路：</strong>一般的解决思路都是这样，进入app就开始检测更新，然后弹出Dialog告诉用户，用户点击更新按钮，开启线程下载文件，同时显示下载进度，下载完成自动弹出安装界面，用户安装完成更新。</p>
<p>检测更新涉及到跟后台的交互，可用json和xml说明新版本信息。<br>其中：</p>
<blockquote>
<p>必须包括的内容有：versionCode版本号、更新说明、下载地址</p>
<p>可能包含的信息有：MD5值、新版本截图等</p>
</blockquote>
<p>请求拿到数据之后对比本地版本号，开始后面的操作。</p>
<p>此处封装了一个完整的类UpdateManager来解决更新问题，以下为逻辑代码，详细实现参看文末Demo</p>
<figure class="highlight java"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div><div class="line">17</div><div class="line">18</div><div class="line">19</div><div class="line">20</div><div class="line">21</div><div class="line">22</div><div class="line">23</div><div class="line">24</div><div class="line">25</div></pre></td><td class="code"><pre><div class="line"></div><div class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">UpdateManager</span> </span>&#123;</div><div class="line"></div><div class="line">	<span class="comment">//构造方法</span></div><div class="line">	<span class="function"><span class="keyword">public</span> <span class="title">UpdateManager</span><span class="params">(Context context)</span> </span>&#123;</div><div class="line">        <span class="keyword">this</span>.mContext = context;</div><div class="line">    &#125;</div><div class="line">    <span class="comment">//检测更新</span></div><div class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">checkUpdate</span><span class="params">()</span> </span>&#123;</div><div class="line">    	<span class="keyword">if</span> (isUpdate()) &#123;</div><div class="line">            <span class="comment">// 显示提示对话框</span></div><div class="line">            showNoticeDialog();</div><div class="line">        &#125;</div><div class="line">    &#125;</div><div class="line">    <span class="comment">//判断更新</span></div><div class="line">    <span class="function"><span class="keyword">private</span> <span class="keyword">boolean</span> <span class="title">isUpdate</span><span class="params">()</span> </span>&#123;&#125;</div><div class="line">    <span class="comment">//显示软件更新对话框</span></div><div class="line">    <span class="function"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title">showNoticeDialog</span><span class="params">()</span> </span>&#123;</div><div class="line">    	<span class="comment">//下载</span></div><div class="line">    	<span class="comment">//显示下载进度</span></div><div class="line">    &#125;</div><div class="line">    <span class="comment">//安装</span></div><div class="line">    <span class="function"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title">installApk</span><span class="params">()</span> </span>&#123;&#125;</div><div class="line">    </div><div class="line">&#125;</div></pre></td></tr></table></figure>
<p>然后只需要在想使用的类里面new出UpdateManage实例对象，在想使用的地方调用checkUpdate方法。<br>这里面有很多比较繁琐的内容，比如发放强制更新版本，绘制美观的更新界面，在状态栏的进度显示等等。</p>
<h2 id="使用腾讯bugly自动更新"><a href="#使用腾讯bugly自动更新" class="headerlink" title="使用腾讯bugly自动更新"></a>使用腾讯bugly自动更新</h2><p>其实参看官方的<a href="https://bugly.qq.com/docs/user-guide/instruction-manual-android-upgrade/?v=20161011192545" target="_blank" rel="external">应用升级SDK说明</a>就很容易集成了，集成方式本身就很简单，教程也给得清楚明了。</p>
<p>这里也是给出思路：</p>
<blockquote>
<p>1.集成jar包或者Gradle依赖，jar包下载和Gradle依赖的写法去官网看哇</p>
<p>2.在想要实现升级的地方配置：</p>
</blockquote>
<pre><code>Bugly.init(getApplicationContext(), &quot;添加产品后出现的APPID&quot;, true);//bugly自动升级
</code></pre><blockquote>
<p>3.Manifest文件加上更新弹窗的activity:</p>
</blockquote>
<pre><code>&lt;!--腾讯bugly自动更新--&gt;
    &lt;activity
        android:name=&quot;com.tencent.bugly.beta.ui.BetaActivity&quot;
        android:theme=&quot;@android:style/Theme.Translucent&quot;/&gt;
</code></pre><blockquote>
<p>4.以及权限配置：</p>
</blockquote>
<pre><code>&lt;uses-permission android:name=&quot;android.permission.READ_PHONE_STATE&quot; /&gt;
&lt;uses-permission android:name=&quot;android.permission.INTERNET&quot; /&gt;
&lt;uses-permission android:name=&quot;android.permission.ACCESS_NETWORK_STATE&quot; /&gt;
&lt;uses-permission android:name=&quot;android.permission.ACCESS_WIFI_STATE&quot; /&gt;
&lt;uses-permission android:name=&quot;android.permission.READ_LOGS&quot; /&gt;
&lt;!--保存资源到SD卡--&gt;
&lt;uses-permission android:name=&quot;android.permission.WRITE_EXTERNAL_STORAGE&quot; /&gt;
</code></pre><blockquote>
<p>5.在bugly后台上传新版本的apk包，并配置更新策略。</p>
</blockquote>
<p>这时候打开低于bugly后台apk里面的versionCode的旧应用，就会在三秒左右的时间里自动检测更新了。</p>
<h2 id="最后的说明"><a href="#最后的说明" class="headerlink" title="最后的说明"></a>最后的说明</h2><p>自己写的要满足要求很麻烦，bugly算是给出来一个比较满意的方案，但是PM和UI一定要求某种效果的话，还是得自己去写这个升级策略了，终极定制化还是得落到自己手里的。</p>
<p>另外,Demo里面的targetSdkVersion是24，所以涉及到动态权限的问题，使用RxPermissions处理了一下。<br><a href="http://www.jianshu.com/p/42b8a1bb7431" target="_blank" rel="external">Android-快速完成6.0运行时动态权限申请
</a></p>
<h4 id="上代码！AutoUpdateDemo-1"><a href="#上代码！AutoUpdateDemo-1" class="headerlink" title="上代码！AutoUpdateDemo"></a>上代码！<a href="https://github.com/paradoxie/AutoUpdateDemo" target="_blank" rel="external">AutoUpdateDemo</a></h4><h1 id="以上"><a href="#以上" class="headerlink" title="以上"></a>以上</h1><hr>
<blockquote>
<p>本文作者：paradoxie</p>
<p>个人主页：<a href="http://www.paradoxie.cf/" target="_blank" rel="external">谢盒盒的小黑屋</a></p>
<p>简书地址：<a href="http://www.jianshu.com/users/05f39939cbf3/latest_articles" target="_blank" rel="external">简书主页</a></p>
<p>github地址：<a href="https://github.com/paradoxie" target="_blank" rel="external">paradoxie</a></p>
<p>转载请注明出处，蟹蟹!</p>
<p>——-我的梦想真的是做一条咸鱼！</p>
</blockquote>
]]></content>
    </entry>
    
    <entry>
      <title><![CDATA[【译】使用约束布局(ConstraintLayout)构建灵活的UI]]></title>
      <url>http://blog.prozin.xyz/2016/09/22/constraintlayout/</url>
      <content type="html"><![CDATA[<p>Google I/O 2016上，Android团队为开发者带来了ConstraintLayout，一种构建于弹性Constraints（约束）系统的新型Android Layout，据说它将大大改变今后的Android布局界面开发方式，据说也许以后Android开发没有那么多布局需要学习，只需要熟悉这一种布局即可。前两天Android studio 2.2正式版发布了，更多的Android 开发者会接触并(可能)使用这个布局。</p>
<a id="more"></a>
<h1 id="前言"><a href="#前言" class="headerlink" title="前言:"></a>前言:</h1><blockquote>
<p>1.本文系<a href="https://developer.android.com/training" target="_blank" rel="external">https://developer.android.com/training</a> 内对约束布局(ConstraintLayout)  文章的翻译</p>
<p>2.阅读本文可以对照原文<a href="https://developer.android.com/training/constraint-layout/index.html#adjust-the-view-margins" target="_blank" rel="external">Build a Responsive UI with ConstraintLayout</a>，科学上网</p>
<p>3.本文可对照实战入门教程食用：<a href="http://www.jianshu.com/p/792d2682c538" target="_blank" rel="external">安卓约束控件(ConstraintLayout)扁平化布局入门</a></p>
<p>4.文章内view均翻译为视图，也可理解为控件</p>
<p>5.文章中涉及的video我还不知道怎么转移哇，要看视频演示的点进原文，还有涉及的一些关键词链接，科学上网你懂的。原文用到的图片我转移到了github，但是github你们也懂的，有时候是真不给力ಥ_ಥ</p>
<p>6.水平有限，翻译上的错误和偏差求指导求轻喷ಥ_ಥ</p>
</blockquote>
<hr>
<h1 id="使用约束布局-ConstraintLayout-构建灵活的UI"><a href="#使用约束布局-ConstraintLayout-构建灵活的UI" class="headerlink" title="使用约束布局(ConstraintLayout)构建灵活的UI"></a>使用约束布局(ConstraintLayout)构建灵活的UI</h1><p>约束布局允许你在相同视图层级中(不是相互层叠的视图组合)制作大而复杂的布局。它类似于相对布局那样所有的视图位置是根据子布局或父布局决定的，但是它比相对布局更加灵活，并且在Android studio布局编辑器中更加易用。<br>你可以直接在布局编辑器的可视化工具中做约束布局中所做的所有事，因为布局API和布局编辑器已经做了特地融合。所以你可以通过拖拽使用约束布局构建你的布局，而不需要再去编辑XML文件。</p>
<p><img src="http://upload-images.jianshu.io/upload_images/532408-56580854d286a98d.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240" alt="图1，布局编辑器中的约束布局"></p>
<p>约束布局可以在兼容Android2.3(API 9)或者更高的API库中使用，而新的布局编辑器则在Android studio2.2中可用。</p>
<p>这篇文章提供了一个在Android studio中使用约束布局构建一个布局的指导。如果你想要了解关于布局编辑器更多信息，在这个 <a href="https://developer.android.com/studio/write/layout-editor.html" target="_blank" rel="external">Build a UI with Layout Editor</a>Android studio指导中查看.</p>
<h1 id="约束概述"><a href="#约束概述" class="headerlink" title="约束概述"></a>约束概述</h1><p>为了定义在约束布局中视图的位置，你必须为视图添加两个或更多约束条件。每个约束代表了和另个视图、父布局或不可见指引线的联系或对齐。每个约束限定了视图是沿着垂直轴还是水平轴的位置，所以每个视图必须在每个轴最少拥有一个约束，但经常需要更多。</p>
<p>当你往布局编辑器中拖拽一个视图时，它会停留在你扔下它的位置，即使它没有任何约束。然鹅，这只是为了使编辑更简单。当一个视图没有任何约束时，你在设备上运行起来后，它会被绘制在左上角[0,0]的位置。</p>
<p>在图2里面，布局在编辑器中看起来不错，但是对于TextView B没有对其进行竖直方向上的约束。当这个布局被绘制到设备上时，TextView B会在水平方向上水平与ImageView的左右边缘对齐，但是出现在屏幕的顶部，因为它没有数值方向上的约束。</p>
<p><img src="http://upload-images.jianshu.io/upload_images/532408-15a78dddc41fc2b0.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240" alt="图2，缺少垂直方向约束的TextView B"></p>
<p><img src="http://upload-images.jianshu.io/upload_images/532408-9b5a11e1d285fe50.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240" alt="图3，TextView B现在在垂直方向上约束于ImageView"></p>
<p>虽然缺少约束条件不会造成编译错误，但布局编辑器会在ToolBar中展示为一个错误指示缺少约束条件。要查看错误或者其他警告，可以点击<a href="">Show Warnings and Errors</a>.为了帮你避免忽视约束条件，布局编辑器会使用<a href="https://developer.android.com/training/constraint-layout/index.html#use-autoconnect-and-infer-constraints" target="_blank" rel="external"> Autoconnect and infer constraints</a>功能为你自动添加约束。</p>
<h1 id="在你的工程中添加约束布局"><a href="#在你的工程中添加约束布局" class="headerlink" title="在你的工程中添加约束布局"></a>在你的工程中添加约束布局</h1><p>要在你的工程中使用约束布局，步骤如下：</p>
<p>1，确定你有最新的Constraint Layout library；</p>
<pre><code>1.点击Tools &gt; Android &gt; SDK Manager.
2.点击 SDK Tools栏.
3.展开Support Repository，然后检查ConstraintLayout for Android，
    并且Solver for ConstraintLayout.
    检查Show Package Details，注意你下载的版本（你需要的是下面这个）。
4.点击OK.
5.在你的module-level build.gradle文件里面添加ConstraintLayout library作为依赖：
</code></pre><figure class="highlight gradle"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line"></div><div class="line"><span class="keyword">dependencies</span> &#123;</div><div class="line">    <span class="keyword">compile</span> <span class="string">'com.android.support.constraint:constraint-layout:1.0.0-alpha8'</span></div><div class="line">&#125;</div></pre></td></tr></table></figure>
<blockquote>
<p>你下载的库版本或许会更高，所以确保你指定的版本跟第三步中的版本相匹配。</p>
</blockquote>
<pre><code>6.在toolbar或者sync提示中，点击Sync Project with Gradle Files.
</code></pre><p>现在你可以使用ConstraintLayout构建布局了.</p>
<h2 id="转换布局"><a href="#转换布局" class="headerlink" title="转换布局"></a>转换布局</h2><p>按照以下步骤，将现有的布局转换为约束布局：</p>
<pre><code>1.在Android Studio打开你的布局，点击编辑器窗口最下面的Design标签.
2.在Component Tree窗口，右键布局，然后点击Convert layout to ConstraintLayout.
</code></pre><p><img src="http://upload-images.jianshu.io/upload_images/532408-1e00744a541d9d27.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240" alt="图4,转换为ConstraintLayout的功能菜单"></p>
<h2 id="新建布局"><a href="#新建布局" class="headerlink" title="新建布局"></a>新建布局</h2><p>以下步骤开始新建一个约束布局文件：</p>
<pre><code>1.在Project窗口任意位置右键，然后选择File &gt; New &gt; XML &gt; Layout XML.
2.输入布局文件名字，然后输入&quot;android.support.constraint.ConstraintLayout&quot;
 作为布局根标签.
3.点击完成.
</code></pre><h2 id="添加约束"><a href="#添加约束" class="headerlink" title="添加约束"></a>添加约束</h2><p>首先从Palette窗口拖拽一个视图到编辑器。当你在约束布局中添加一个视图时，它显示为一个每个角都带有正方形手柄，并且每边都带有的环形手柄的边框</p>
<p>点击选中视图，然后点击并按住约束手柄之一并拖动线到可用的锚点(另一个视图的边缘，布局的边缘，或者一条引导线)。当你释放的时候，约束生成，带有默认的<a href="https://developer.android.com/training/constraint-layout/index.html#adjust-the-view-margins" target="_blank" rel="external">a default margin</a>分离两个视图。</p>
<p><a href="https://storage.googleapis.com/androiddevelopers/videos/studio/studio-constraint-first.mp4" target="_blank" rel="external">video1,视图左侧被约束到布局左侧</a></p>
<p>video1</p>
<p>新建约束时，记住以下规则：</p>
<figure class="highlight gams"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div></pre></td><td class="code"><pre><div class="line"><span class="comment">*每一个视图至少有两个约束：一个垂直方向，一个水平方向。</span></div><div class="line"><span class="comment">*只有在约束手柄和锚点在同一平面时才能新建约束，</span></div><div class="line">	所以一个视图的垂直平面(左右侧)只能被约束到另一垂直平面。</div><div class="line">	并且基线只能约束到其他基线。</div><div class="line"><span class="comment">*每个约束手柄只能被用于一个约束，但是可以对同一个锚点创建多个约束(从不同的视图)</span></div></pre></td></tr></table></figure>
<blockquote>
<p>选中一个视图然后点击约束手柄，可以删除一个约束。</p>
</blockquote>
<p>如果你在一个视图中添加对立的约束，约束线变成像弹簧一样的波浪来表示反动势力，就像<a href="https://storage.googleapis.com/androiddevelopers/videos/studio/studio-constraint-second.mp4" target="_blank" rel="external">video2</a>中展示的一样。</p>
<p>当视图的大小被设为『fixed』或者『wrap content』时，这种情况下视图位于约束条件中心时，效果最为明显。如果你希望视图撑开尺寸以满足约束条件，改变尺寸至”any size”；或者如果想保持目前的大小，单移动视图，使其不居中，可以调整约束偏置。</p>
<h1 id="有许多方式可以来约束一个视图，但下面的约束类型提供了基本构建模板"><a href="#有许多方式可以来约束一个视图，但下面的约束类型提供了基本构建模板" class="headerlink" title="有许多方式可以来约束一个视图，但下面的约束类型提供了基本构建模板"></a>有许多方式可以来约束一个视图，但下面的约束类型提供了基本构建模板</h1><h2 id="父约束"><a href="#父约束" class="headerlink" title="父约束"></a>父约束</h2><p>视图的侧面连接到相应布局的边缘。</p>
<p><img src="http://upload-images.jianshu.io/upload_images/532408-4db943ba21b8a160.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240" alt="图5"></p>
<p>图5中，视图的左边被连接到父布局的左边缘.</p>
<h2 id="位置约束"><a href="#位置约束" class="headerlink" title="位置约束"></a>位置约束</h2><p>给两个视图定义出现的次序，垂直或者水平。</p>
<p><img src="http://upload-images.jianshu.io/upload_images/532408-8864929ec98e938c.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240" alt="图6"></p>
<p>图6中，Button被约束于ImageView下边缘24dp处，</p>
<h2 id="对齐约束"><a href="#对齐约束" class="headerlink" title="对齐约束"></a>对齐约束</h2><p>对齐视图的边缘到其他视图的同意边缘。</p>
<p><img src="http://upload-images.jianshu.io/upload_images/532408-8da3d7f3aaf8a0d3.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240" alt="图7"></p>
<p>图7中，Button的左边缘被对齐到ImageView的左边缘。<br>你可以从约束向里拖动视图校准偏移。比如，在</p>
<p><img src="http://upload-images.jianshu.io/upload_images/532408-ba2409311b7a46b8.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240" alt="图8"></p>
<p>图8中，展示了相同的Button偏移了24dp，这是通过约束视图边界限定的偏移。</p>
<h2 id="基线对齐约束"><a href="#基线对齐约束" class="headerlink" title="基线对齐约束"></a>基线对齐约束</h2><p>对齐视图的文本基线到其他视图的文本基线。</p>
<p><img src="http://upload-images.jianshu.io/upload_images/532408-e59f711d399ed75b.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240" alt="图9"></p>
<p>图9中，第一行的TextView被对齐到Button的text。<br>将鼠标悬停在基线手柄两秒钟，直至手柄闪烁白色。然后点击并拖出一条线到另一个基线，创建出一个基线约束。</p>
<h2 id="引导线约束"><a href="#引导线约束" class="headerlink" title="引导线约束"></a>引导线约束</h2><p>在想附加约束的地方可以添加水平或垂直的引导线，你可以在布局内部基于相对于布局边缘的dp或是百分比定位引导线。</p>
<p>在toolbar中点击Guidelines，然后再点击Add Vertical Guidelin或者Add Horizontal Guideline创建引导线。</p>
<p>点击引导线边缘的环形toggle，切换用于定位引导线的测量值(从布局边缘的百分比或dp单位)</p>
<p>引导线不会对用户可见。</p>
<p><a href="https://storage.googleapis.com/androiddevelopers/videos/studio/add-layout-guideline_2-2.mp4" target="_blank" rel="external">video3</a></p>
<h2 id="使用自动连接并推断约束"><a href="#使用自动连接并推断约束" class="headerlink" title="使用自动连接并推断约束"></a>使用自动连接并推断约束</h2><p>自动连接是一个持久的模式，会为你添加到布局中的每一个视图自动创建两个或多个约束，默认为不可使用状态。你可以在布局编辑器的ToolBar中点击Turn on Autoconnect来启用。</p>
<p>当其可用时，你添加的每个视图都会为你自动创建约束。它不会对布局中已经存在的视图创建约束。一旦约束创建，如果你想拖拽视图,约束不会改变。所以如果你想明显的重新定位视图的位置，必须先删除约束。</p>
<p>或者，你可以点击Infer Constraints为布局中所有的视图创建约束。</p>
<p>推断约束（Infer Constraints）是一次性的动作，以扫描整个布局以确定所有视图最有效的约束，因此它可能创建彼此相隔很远之间的约束。然鹅，自动连接(Autoconnect)创建的约束只是在你添加的视图上，并且它创建的约束只会在附近的元素上。这两种情况下，你可以随时通过点击约束手柄删除修改约束，然后创建一个新的约束。</p>
<p><a href="https://storage.googleapis.com/androiddevelopers/videos/studio/constraint-autoconnect_2-2.mp4" target="_blank" rel="external">video4</a></p>
<h2 id="调整视图大小"><a href="#调整视图大小" class="headerlink" title="调整视图大小"></a>调整视图大小</h2><p>你可以用视图每个角的额手柄来改变视图的大小，但是你应该规避对大多数视图做宽度和高度的硬编码，应为硬编码视图大小无法适应不同的内容和屏幕尺寸。点击视图，打开编辑器右侧的Properties窗口，选择一种动态调整大小的模式或者定义更多具体的尺寸。如</p>
<p><img src="http://upload-images.jianshu.io/upload_images/532408-ea29b54fe5488be1.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240" alt="图10，Properties窗口包括①视图尺寸，②边距，③约束偏置"></p>
<p>图10，Properties窗口包括①视图尺寸，②边距，③约束偏置,在窗口顶部是视图检查器。</p>
<p>灰色方框代表选中的视图，方框内的符号(卧槽这个符号我就用字符代替了哈ಠ౪ಠ)表示宽度和高度值可以用如下方式设置：</p>
<pre><code>* &gt;&gt;&gt;Wrap Content:视图自适应为其内容大小。
* |-/|/|/-|Any Size:视图按照匹配的约束适应大小。
    视图没有指定明确的尺寸，实际值为0dp，但是它按照满足需求的约束调整了大小。
    然而，如果给定的尺寸只有一个约束，那么该视图自适应其内容。
    另一种思路可以认为它是&quot;match constraints&quot;（等同于match_parent）。
    因为它会在满足每个约束和边距条件后，尽可能的扩展视图。
*|---|Fixed:在文本框中指定尺寸，或者在编辑器中调整视图大小。
</code></pre><p>点击符号，可在这些设置之间切换。</p>
<blockquote>
<p>Note:在约束布局中的所有视图，都应该使用match_parent，而不是 “Any Size” (0dp)。</p>
</blockquote>
<h2 id="调整约束偏置"><a href="#调整约束偏置" class="headerlink" title="调整约束偏置"></a>调整约束偏置</h2><p>当你在一个视图（相同尺寸的视图大小为”fixed”或”wrap content”）两侧添加一个约束时，默认情况下该视图成为两个锚点的中心，偏置为50%。你可以通过在Properties窗口中拖动偏置滑块或者直接拖动视图来调整偏置，如<a href="https://storage.googleapis.com/androiddevelopers/videos/studio/adjust-constraint-bias.mp4" target="_blank" rel="external">video5,调整约束偏置</a>.</p>
<p>如果你想让视图满足约束的情况下撑开大小，切换视图尺寸至任何大小”any size”。</p>
<p>##调整视图边距</p>
<p>为了确保你所有的视图是均匀分布，点击ToolBar中的Margin 8，对你添加进布局的视图选择默认边距。这个按钮会变为显示你当前边距的选项，你对默认边距的修改只会应用于修改之后添加的所有的视图。</p>
<p><img src="http://upload-images.jianshu.io/upload_images/532408-98f2549e2ac45200.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240" alt="图11，ToolBar的Margin按钮，点击调整默认边距"></p>
<p>你可以通过点击在Properties窗口里面代表约束线上面的数字，来控制每个视图的边距（图10中，每个边距被设置为16dp）.</p>
<p>工具提供的所有边距为8dp的原因是为了让你的视图跟随Material Design大法的<a href="https://material.google.com/layout/metrics-keylines.html" target="_blank" rel="external"> 8dp square grid recommendations</a></p>
<h1 id="以上"><a href="#以上" class="headerlink" title="以上"></a>以上</h1><hr>
<blockquote>
<p>本文作者：paradoxie</p>
<p>个人主页：<a href="http://www.paradoxie.cf/" target="_blank" rel="external">谢盒盒的小黑屋</a></p>
<p>简书地址：<a href="http://www.jianshu.com/users/05f39939cbf3/latest_articles" target="_blank" rel="external">简书主页</a></p>
<p>github地址：<a href="https://github.com/paradoxie" target="_blank" rel="external">paradoxie</a></p>
<p>转载请注明出处，蟹蟹!</p>
<p>——-我的梦想真的是做一条咸鱼！</p>
</blockquote>
]]></content>
    </entry>
    
    <entry>
      <title><![CDATA[环信IM问题汇总]]></title>
      <url>http://blog.prozin.xyz/2016/09/18/%E7%8E%AF%E4%BF%A1IM%E9%97%AE%E9%A2%98%E6%B1%87%E6%80%BB/</url>
      <content type="html"><![CDATA[<h1 id="好吧不光是Android的，好多iOS的问题…-不过这不重要…"><a href="#好吧不光是Android的，好多iOS的问题…-不过这不重要…" class="headerlink" title="好吧不光是Android的，好多iOS的问题….不过这不重要….."></a>好吧不光是Android的，好多iOS的问题….不过这不重要…..</h1><p>之前看到了这个，觉得提供了很多小问题的解决方向，但是排版有点辣眼睛呐，所以自己弄一份，方便查阅。</p>
<a id="more"></a>
<hr>
<h4 id="1）项目里面不需要环信SDK的太多功能，只是想要聊天和好友功能，其他都不用，那SDK一定要总是跟着更新么？"><a href="#1）项目里面不需要环信SDK的太多功能，只是想要聊天和好友功能，其他都不用，那SDK一定要总是跟着更新么？" class="headerlink" title="1）项目里面不需要环信SDK的太多功能，只是想要聊天和好友功能，其他都不用，那SDK一定要总是跟着更新么？"></a>1）项目里面不需要环信SDK的太多功能，只是想要聊天和好友功能，其他都不用，那SDK一定要总是跟着更新么？</h4><figure class="highlight stylus"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line"><span class="selector-tag">a</span>.环信SDK版本都是向前兼容并且稳定的，每次更新安卓只要更新.a包即可。</div><div class="line"><span class="selector-tag">b</span>.每次更新的版本都是稳定可靠性的，可以放心使用。</div></pre></td></tr></table></figure>
<hr>
<h4 id="2）A用户通过Rest接口添加B为好友，B的客户端上好友列表上是否有？"><a href="#2）A用户通过Rest接口添加B为好友，B的客户端上好友列表上是否有？" class="headerlink" title="2）A用户通过Rest接口添加B为好友，B的客户端上好友列表上是否有？"></a>2）A用户通过Rest接口添加B为好友，B的客户端上好友列表上是否有？</h4><figure class="highlight dns"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">对的，B那边也添加<span class="keyword">A</span>为好友了。</div></pre></td></tr></table></figure>
<hr>
<h4 id="3）黄某某（产品事业部）中，怎么把文字取出来"><a href="#3）黄某某（产品事业部）中，怎么把文字取出来" class="headerlink" title="3）黄某某（产品事业部）中，怎么把文字取出来?"></a>3）黄某某（产品事业部）中，怎么把文字取出来?</h4><figure class="highlight lasso"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">用<span class="built_in">XML</span>解析出来。</div></pre></td></tr></table></figure>
<hr>
<h4 id="4）请问一下apns可以设置自定义声音么？"><a href="#4）请问一下apns可以设置自定义声音么？" class="headerlink" title="4）请问一下apns可以设置自定义声音么？"></a>4）请问一下apns可以设置自定义声音么？</h4><figure class="highlight"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">环信的推送不支持自定义，只可以设置开和关，不可以自定义声音，</div><div class="line">如果有需要可以使用第三方的推送。</div></pre></td></tr></table></figure>
<hr>
<h4 id="5）iOS的demo中删除好友，聊天记录删除的只是一方好友的聊天记录么？"><a href="#5）iOS的demo中删除好友，聊天记录删除的只是一方好友的聊天记录么？" class="headerlink" title="5）iOS的demo中删除好友，聊天记录删除的只是一方好友的聊天记录么？"></a>5）iOS的demo中删除好友，聊天记录删除的只是一方好友的聊天记录么？</h4><figure class="highlight"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">聊天记录不会删除，只是不在UI上显示出来了而已。</div></pre></td></tr></table></figure>
<hr>
<h4 id="6）环信支持-功能么？"><a href="#6）环信支持-功能么？" class="headerlink" title="6）环信支持@功能么？"></a>6）环信支持@功能么？</h4><figure class="highlight"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">不支持，需要自己去实现。</div></pre></td></tr></table></figure>
<hr>
<h4 id="7）发送消息的时候，没有网络，就会马上显示发送不成功，环信会不会像QQ那样有个自己去持续请求的时间呢？"><a href="#7）发送消息的时候，没有网络，就会马上显示发送不成功，环信会不会像QQ那样有个自己去持续请求的时间呢？" class="headerlink" title="7）发送消息的时候，没有网络，就会马上显示发送不成功，环信会不会像QQ那样有个自己去持续请求的时间呢？"></a>7）发送消息的时候，没有网络，就会马上显示发送不成功，环信会不会像QQ那样有个自己去持续请求的时间呢？</h4><figure class="highlight"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">没有，发送不成功就需要点击叹号重新发送。</div></pre></td></tr></table></figure>
<hr>
<h4 id="8）一个环信应用可以注册多个推送证书吗？"><a href="#8）一个环信应用可以注册多个推送证书吗？" class="headerlink" title="8）一个环信应用可以注册多个推送证书吗？"></a>8）一个环信应用可以注册多个推送证书吗？</h4><figure class="highlight"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">可以注册多个推送证书。</div></pre></td></tr></table></figure>
<hr>
<h4 id="9）环信发送图片那里是断点发送的？如果断开了，隔了一段时间，怎么重连？"><a href="#9）环信发送图片那里是断点发送的？如果断开了，隔了一段时间，怎么重连？" class="headerlink" title="9）环信发送图片那里是断点发送的？如果断开了，隔了一段时间，怎么重连？"></a>9）环信发送图片那里是断点发送的？如果断开了，隔了一段时间，怎么重连？</h4><figure class="highlight"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">图片发送不会断点续传，发送不成功会出现一个叹号，点击之后会重新发送。</div></pre></td></tr></table></figure>
<hr>
<h4 id="10）如果没有注册过的账号去调用环信的登录接口，是不是也能连接聊天服务器？"><a href="#10）如果没有注册过的账号去调用环信的登录接口，是不是也能连接聊天服务器？" class="headerlink" title="10）如果没有注册过的账号去调用环信的登录接口，是不是也能连接聊天服务器？"></a>10）如果没有注册过的账号去调用环信的登录接口，是不是也能连接聊天服务器？</h4><figure class="highlight"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">不是，没有注册的用户不可以聊天。</div></pre></td></tr></table></figure>
<hr>
<h4 id="11）在哪设置气泡高度"><a href="#11）在哪设置气泡高度" class="headerlink" title="11）在哪设置气泡高度"></a>11）在哪设置气泡高度</h4><figure class="highlight"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">在sizeThatFits中设置。</div></pre></td></tr></table></figure>
<hr>
<h4 id="12）环信怎么判断当前用户是否登陆？"><a href="#12）环信怎么判断当前用户是否登陆？" class="headerlink" title="12）环信怎么判断当前用户是否登陆？"></a>12）环信怎么判断当前用户是否登陆？</h4><figure class="highlight mathematica"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">环信不关注用户是否登录，类似微信一样，就算不在线也可以推送，</div><div class="line">只可以通过<span class="keyword">Rest</span>在获取好友详情的时候，看到好友是online还是offline。</div></pre></td></tr></table></figure>
<hr>
<h4 id="13）用户在登录情况下后台显示是有证书的，是不是已退出账号后证书名称在后台就显示为空了？"><a href="#13）用户在登录情况下后台显示是有证书的，是不是已退出账号后证书名称在后台就显示为空了？" class="headerlink" title="13）用户在登录情况下后台显示是有证书的，是不是已退出账号后证书名称在后台就显示为空了？"></a>13）用户在登录情况下后台显示是有证书的，是不是已退出账号后证书名称在后台就显示为空了？</h4><figure class="highlight"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">对的，登陆的时候在后台会显示，退出之后就不显示了。</div></pre></td></tr></table></figure>
<hr>
<h4 id="14）如何设置语音的时长限制？"><a href="#14）如何设置语音的时长限制？" class="headerlink" title="14）如何设置语音的时长限制？"></a>14）如何设置语音的时长限制？</h4><figure class="highlight"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">这个需要开发者自己去设置，可以做一个用当前录制时间减去开始录制时间的判断。</div><div class="line">另外，音频部分已经完全剥离出来，SDK已经不做了，全部放到UI上供开发者自行控制操作。</div></pre></td></tr></table></figure>
<hr>
<h4 id="15）发送消息后-actity一直在打转，是怎么回事？"><a href="#15）发送消息后-actity一直在打转，是怎么回事？" class="headerlink" title="15）发送消息后 actity一直在打转，是怎么回事？"></a>15）发送消息后 actity一直在打转，是怎么回事？</h4><figure class="highlight"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">需要在发送成功的回调方法里面刷新UI。</div></pre></td></tr></table></figure>
<hr>
<h4 id="16）在服务端通过rest-删除的好友的话，环信的server-端会通知client端吗？"><a href="#16）在服务端通过rest-删除的好友的话，环信的server-端会通知client端吗？" class="headerlink" title="16）在服务端通过rest 删除的好友的话，环信的server 端会通知client端吗？"></a>16）在服务端通过rest 删除的好友的话，环信的server 端会通知client端吗？</h4><figure class="highlight"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">会的。</div></pre></td></tr></table></figure>
<hr>
<h4 id="17）ios-可以重复建群吗？同样的三个人可以建多个群吗？"><a href="#17）ios-可以重复建群吗？同样的三个人可以建多个群吗？" class="headerlink" title="17）ios 可以重复建群吗？同样的三个人可以建多个群吗？"></a>17）ios 可以重复建群吗？同样的三个人可以建多个群吗？</h4><figure class="highlight"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">可以重复建群。</div></pre></td></tr></table></figure>
<hr>
<h4 id="18）环信有类似微信朋友圈的模板？"><a href="#18）环信有类似微信朋友圈的模板？" class="headerlink" title="18）环信有类似微信朋友圈的模板？"></a>18）环信有类似微信朋友圈的模板？</h4><figure class="highlight"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">目前还没有朋友圈的模板。</div></pre></td></tr></table></figure>
<hr>
<h4 id="19）假如用户在两台ios设置上互相踢下线，要怎么更新最后登陆设置的推送设置？"><a href="#19）假如用户在两台ios设置上互相踢下线，要怎么更新最后登陆设置的推送设置？" class="headerlink" title="19）假如用户在两台ios设置上互相踢下线，要怎么更新最后登陆设置的推送设置？"></a>19）假如用户在两台ios设置上互相踢下线，要怎么更新最后登陆设置的推送设置？</h4><figure class="highlight stata"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">在用户登录的时候，会把用的证书名发到后台，后台就会把证书和<span class="keyword">app</span>当前用户绑定。</div></pre></td></tr></table></figure>
<hr>
<h4 id="20）环信SDK支持离线消息吗？"><a href="#20）环信SDK支持离线消息吗？" class="headerlink" title="20）环信SDK支持离线消息吗？"></a>20）环信SDK支持离线消息吗？</h4><figure class="highlight erlang"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">支持的，只要用户在线，则直接把消息发送过去.</div><div class="line">ios端如果不在线则通过apns推送过去的.</div><div class="line">Android端如果不在线则聊天消息先保存在服务器上，等Android登录后直接推送过去。</div></pre></td></tr></table></figure>
<hr>
<h4 id="21）如何设置让环信推送显示昵称而不是username呢？"><a href="#21）如何设置让环信推送显示昵称而不是username呢？" class="headerlink" title="21）如何设置让环信推送显示昵称而不是username呢？"></a>21）如何设置让环信推送显示昵称而不是username呢？</h4><figure class="highlight css"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">设置方法<span class="selector-attr">[[EaseMob sharedInstance]</span><span class="selector-class">.chatManager</span> <span class="selector-tag">setApnsNickname</span>:@"<span class="keyword">APNS</span>昵称"]。</div></pre></td></tr></table></figure>
<hr>
<h4 id="22）apns上传的是什么文件？"><a href="#22）apns上传的是什么文件？" class="headerlink" title="22）apns上传的是什么文件？"></a>22）apns上传的是什么文件？</h4><figure class="highlight armasm"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line"><span class="symbol">apns</span>上传的是<span class="built_in">p12</span>，并且需要密码。</div></pre></td></tr></table></figure>
<hr>
<h4 id="23）环信的表情可以换成app的吗"><a href="#23）环信的表情可以换成app的吗" class="headerlink" title="23）环信的表情可以换成app的吗?"></a>23）环信的表情可以换成app的吗?</h4><figure class="highlight xml"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">表情就是字符串替换。字符串环信没有定义， app里协定好了就可以。</div><div class="line">比如<span class="tag">&lt;<span class="name">:)</span>&gt;</span>  这个字符串代表笑脸的图片，ui上截取了就替换就可以了。</div><div class="line">对于环信来说，发的就是一个字符串。</div></pre></td></tr></table></figure>
<hr>
<h4 id="24）怎么判断某条消息是别人发的还是自己发的？"><a href="#24）怎么判断某条消息是别人发的还是自己发的？" class="headerlink" title="24）怎么判断某条消息是别人发的还是自己发的？"></a>24）怎么判断某条消息是别人发的还是自己发的？</h4><figure class="highlight applescript"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">用当前登录人的环信<span class="built_in">id</span>和message的<span class="keyword">from</span>做对比，如果一样，就是自己发的，反之，是对方。</div></pre></td></tr></table></figure>
<hr>
<h4 id="25）-NSNotificationCenter-defaultCenter-addObserver-self-selector-selector-loginStateChange-name-KNOTIFICATION-LOGINCHANGE-object-nil-什么时候触发？"><a href="#25）-NSNotificationCenter-defaultCenter-addObserver-self-selector-selector-loginStateChange-name-KNOTIFICATION-LOGINCHANGE-object-nil-什么时候触发？" class="headerlink" title="25）[[NSNotificationCenter defaultCenter] addObserver:self    selector:@selector(loginStateChange:)    name:KNOTIFICATION_LOGINCHANGE object:nil]; 什么时候触发？"></a>25）[[NSNotificationCenter defaultCenter] addObserver:self    selector:@selector(loginStateChange:)    name:KNOTIFICATION_LOGINCHANGE object:nil]; 什么时候触发？</h4><figure class="highlight"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">登录状态改变才会触发的通知：登录的时候，退出登录的时候，要退出的时候。</div></pre></td></tr></table></figure>
<hr>
<h4 id="26）-EaseMob-sharedInstance-chatManager-setIsAutoLoginEnabled-YES-这个应该写到哪里呢？"><a href="#26）-EaseMob-sharedInstance-chatManager-setIsAutoLoginEnabled-YES-这个应该写到哪里呢？" class="headerlink" title="26）[[EaseMob sharedInstance].chatManager setIsAutoLoginEnabled:YES]; 这个应该写到哪里呢？"></a>26）[[EaseMob sharedInstance].chatManager setIsAutoLoginEnabled:YES]; 这个应该写到哪里呢？</h4><figure class="highlight"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">要想直接设置自动登录， 就在用户第一次登录成功之后设置。</div></pre></td></tr></table></figure>
<hr>
<h4 id="27）要发透传信息，是不是先要把推送证书配置好-？"><a href="#27）要发透传信息，是不是先要把推送证书配置好-？" class="headerlink" title="27）要发透传信息，是不是先要把推送证书配置好 ？"></a>27）要发透传信息，是不是先要把推送证书配置好 ？</h4><figure class="highlight"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">发透传不用推送证书的，发推送才要证书的。</div></pre></td></tr></table></figure>
<hr>
<h4 id="28）环信能自己设置心跳包么？180s有点长？"><a href="#28）环信能自己设置心跳包么？180s有点长？" class="headerlink" title="28）环信能自己设置心跳包么？180s有点长？"></a>28）环信能自己设置心跳包么？180s有点长？</h4><figure class="highlight"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">不可以。</div></pre></td></tr></table></figure>
<hr>
<h4 id="29）环信的心跳包每180s发一次，怎么能保证实时接收对么发来的消息？是不是做了监听？"><a href="#29）环信的心跳包每180s发一次，怎么能保证实时接收对么发来的消息？是不是做了监听？" class="headerlink" title="29）环信的心跳包每180s发一次，怎么能保证实时接收对么发来的消息？是不是做了监听？"></a>29）环信的心跳包每180s发一次，怎么能保证实时接收对么发来的消息？是不是做了监听？</h4><figure class="highlight"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">有监听回调方法。</div></pre></td></tr></table></figure>
<hr>
<h4 id="30）语音怎么过段时间就播放不了？一直显示正在下载语言-，稍后点击-？"><a href="#30）语音怎么过段时间就播放不了？一直显示正在下载语言-，稍后点击-？" class="headerlink" title="30）语音怎么过段时间就播放不了？一直显示正在下载语言 ，稍后点击 ？"></a>30）语音怎么过段时间就播放不了？一直显示正在下载语言 ，稍后点击 ？</h4><figure class="highlight maxima"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line"><span class="built_in">demo</span>的话， 如果是自己发送的话，就不要验证是否下载了，可以直接播放。</div><div class="line">如果是对方的话，要判断一下。</div></pre></td></tr></table></figure>
<hr>
<h4 id="31）历史数据会有显示不完全的问题，怎么办？"><a href="#31）历史数据会有显示不完全的问题，怎么办？" class="headerlink" title="31）历史数据会有显示不完全的问题，怎么办？"></a>31）历史数据会有显示不完全的问题，怎么办？</h4><figure class="highlight excel"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">EMConversation类，有个loadAllMessages,</div><div class="line">调用该方法就可以获取到存入<span class="built_in">db</span>的所有消息。</div></pre></td></tr></table></figure>
<hr>
<h4 id="32）透传消息是不是有文字个数限制？"><a href="#32）透传消息是不是有文字个数限制？" class="headerlink" title="32）透传消息是不是有文字个数限制？"></a>32）透传消息是不是有文字个数限制？</h4><figure class="highlight lsl"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">要求在<span class="number">30</span>kb内, 没有个数限制。</div></pre></td></tr></table></figure>
<hr>
<h4 id="33）怎么让未读消息处于监听模式呢？"><a href="#33）怎么让未读消息处于监听模式呢？" class="headerlink" title="33）怎么让未读消息处于监听模式呢？"></a>33）怎么让未读消息处于监听模式呢？</h4><figure class="highlight actionscript"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">未读消息数改变是有回调方法的--(<span class="keyword">void</span>)didUnreadMessagesCountChanged。</div></pre></td></tr></table></figure>
<hr>
<h4 id="34）群主能更改吗？"><a href="#34）群主能更改吗？" class="headerlink" title="34）群主能更改吗？"></a>34）群主能更改吗？</h4><figure class="highlight"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">目前还不能，但将来会增加这个接口。</div></pre></td></tr></table></figure>
<hr>
<h4 id="35）图片压缩比用户能调整么？"><a href="#35）图片压缩比用户能调整么？" class="headerlink" title="35）图片压缩比用户能调整么？"></a>35）图片压缩比用户能调整么？</h4><figure class="highlight bash"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">可以的，这个是iOS的压缩方法<span class="built_in">set</span>CompressionQuality。</div></pre></td></tr></table></figure>
<hr>
<h4 id="36）环信里面-android和ios-里面-message的扩展字段是通用的吗？"><a href="#36）环信里面-android和ios-里面-message的扩展字段是通用的吗？" class="headerlink" title="36）环信里面 android和ios 里面 message的扩展字段是通用的吗？"></a>36）环信里面 android和ios 里面 message的扩展字段是通用的吗？</h4><figure class="highlight"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">是的。</div></pre></td></tr></table></figure>
<hr>
<h4 id="37）iOS和安卓都用的百度地图吗？"><a href="#37）iOS和安卓都用的百度地图吗？" class="headerlink" title="37）iOS和安卓都用的百度地图吗？"></a>37）iOS和安卓都用的百度地图吗？</h4><figure class="highlight"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">安卓是百度地图，iOS是苹果自己的地图。</div></pre></td></tr></table></figure>
<hr>
<h4 id="38）手机端加了个消息免打扰功能，当webim-和手机端都登陆时可以通过这个功能设置手机是否接受信息么？"><a href="#38）手机端加了个消息免打扰功能，当webim-和手机端都登陆时可以通过这个功能设置手机是否接受信息么？" class="headerlink" title="38）手机端加了个消息免打扰功能，当webim 和手机端都登陆时可以通过这个功能设置手机是否接受信息么？"></a>38）手机端加了个消息免打扰功能，当webim 和手机端都登陆时可以通过这个功能设置手机是否接受信息么？</h4><figure class="highlight"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">不可以，免打扰只是设置推送免打扰。</div></pre></td></tr></table></figure>
<hr>
<h4 id="39）做了个测试，先用一个手机发了很多信息-，然后用另一个手机登这个账号时，发现获取的聊天记录只有2条，而且还是以前该账号在该手机上发的？"><a href="#39）做了个测试，先用一个手机发了很多信息-，然后用另一个手机登这个账号时，发现获取的聊天记录只有2条，而且还是以前该账号在该手机上发的？" class="headerlink" title="39）做了个测试，先用一个手机发了很多信息 ，然后用另一个手机登这个账号时，发现获取的聊天记录只有2条，而且还是以前该账号在该手机上发的？"></a>39）做了个测试，先用一个手机发了很多信息 ，然后用另一个手机登这个账号时，发现获取的聊天记录只有2条，而且还是以前该账号在该手机上发的？</h4><figure class="highlight"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">消息记录保存到本地了，所以换个手机看不到。</div></pre></td></tr></table></figure>
<hr>
<h4 id="40）不是给每个用户分配100m，-超过100M的数据能获取吗？"><a href="#40）不是给每个用户分配100m，-超过100M的数据能获取吗？" class="headerlink" title="40）不是给每个用户分配100m， 超过100M的数据能获取吗？"></a>40）不是给每个用户分配100m， 超过100M的数据能获取吗？</h4><figure class="highlight lsl"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">超过<span class="number">100</span>M新的记录会顶掉老的记录。</div></pre></td></tr></table></figure>
<hr>
<h4 id="41）环信-SDK支持模拟器吗？还是必须要用真机来测试？"><a href="#41）环信-SDK支持模拟器吗？还是必须要用真机来测试？" class="headerlink" title="41）环信 SDK支持模拟器吗？还是必须要用真机来测试？"></a>41）环信 SDK支持模拟器吗？还是必须要用真机来测试？</h4><figure class="highlight"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">支持，但是有些功能只能真机测试，比如推送，视频等。</div></pre></td></tr></table></figure>
<hr>
<h4 id="42）IOS-中如何获取某个聊天组中的成员列表？"><a href="#42）IOS-中如何获取某个聊天组中的成员列表？" class="headerlink" title="42）IOS 中如何获取某个聊天组中的成员列表？"></a>42）IOS 中如何获取某个聊天组中的成员列表？</h4><figure class="highlight"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">用 didFetchGroupOccupantsList方法获取。</div></pre></td></tr></table></figure>
<hr>
<h4 id="43）环信这边语音能不能像微信那样：靠近耳朵的时候就听筒，远离耳朵的时候就扬声器播放啊？"><a href="#43）环信这边语音能不能像微信那样：靠近耳朵的时候就听筒，远离耳朵的时候就扬声器播放啊？" class="headerlink" title="43）环信这边语音能不能像微信那样：靠近耳朵的时候就听筒，远离耳朵的时候就扬声器播放啊？"></a>43）环信这边语音能不能像微信那样：靠近耳朵的时候就听筒，远离耳朵的时候就扬声器播放啊？</h4><figure class="highlight groovy"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">以，要在- (<span class="keyword">void</span>)<span class="string">proximitySensorChanged:</span>(BOOL)isCloseToUser方法中设置。</div></pre></td></tr></table></figure>
<hr>
<h4 id="44）能不能查看离线消息？"><a href="#44）能不能查看离线消息？" class="headerlink" title="44）能不能查看离线消息？"></a>44）能不能查看离线消息？</h4><figure class="highlight"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">不能，只能查看离线消息未读数。</div></pre></td></tr></table></figure>
<hr>
<h4 id="45）读取临时会话列表的方法，有的对象里username-是null，这是为什么？"><a href="#45）读取临时会话列表的方法，有的对象里username-是null，这是为什么？" class="headerlink" title="45）读取临时会话列表的方法，有的对象里username 是null，这是为什么？"></a>45）读取临时会话列表的方法，有的对象里username 是null，这是为什么？</h4><figure class="highlight actionscript"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">创建会话的时候，是需要chatter 这个对象，chatter不能为<span class="literal">null</span> ，</div><div class="line">可以去打印下chatter，判断是否为空。</div></pre></td></tr></table></figure>
<hr>
<h4 id="46）想删除客户端本地聊天记录，怎么办？"><a href="#46）想删除客户端本地聊天记录，怎么办？" class="headerlink" title="46）想删除客户端本地聊天记录，怎么办？"></a>46）想删除客户端本地聊天记录，怎么办？</h4><figure class="highlight objectivec"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">这是删除聊天记录的方法（参考）：</div><div class="line"> -(<span class="built_in">NSUInteger</span>)removeConversationsByChatters:</div><div class="line">  (<span class="built_in">NSArray</span>)chattersdeleteMessages:(<span class="built_in">BOOL</span>)aDeleteMessages。</div></pre></td></tr></table></figure>
<hr>
<h4 id="47）后台企业管理员可以更改吗？"><a href="#47）后台企业管理员可以更改吗？" class="headerlink" title="47）后台企业管理员可以更改吗？"></a>47）后台企业管理员可以更改吗？</h4><figure class="highlight"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">后台企业管理员不能直接删除，邮箱也不可以直接修改，</div><div class="line">但是可以用新的邮箱再添加一个管理，然后把旧的管理员从这个org下移除掉。</div></pre></td></tr></table></figure>
<hr>
<h4 id="48）上传图片后，返回的路径没办法下载图片，什么原因？"><a href="#48）上传图片后，返回的路径没办法下载图片，什么原因？" class="headerlink" title="48）上传图片后，返回的路径没办法下载图片，什么原因？"></a>48）上传图片后，返回的路径没办法下载图片，什么原因？</h4><figure class="highlight qml"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">为了安全，下载过程中还要加入其他参数。</div><div class="line">不是只通过<span class="built_in">url</span>就能下载的。需要参数（share-secret）。</div></pre></td></tr></table></figure>
<hr>
<h4 id="49）NSArray-buddyList-EaseMob-sharedInstance-chatManager-buddyList-获取到的都是空的，什么原因？"><a href="#49）NSArray-buddyList-EaseMob-sharedInstance-chatManager-buddyList-获取到的都是空的，什么原因？" class="headerlink" title="49）NSArray buddyList = [[EaseMob sharedInstance].chatManager buddyList]; 获取到的都是空的，什么原因？"></a>49）NSArray buddyList = [[EaseMob sharedInstance].chatManager buddyList]; 获取到的都是空的，什么原因？</h4><figure class="highlight 1c"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line"><span class="string">"这个是取内存值，要得到detail，要去服务器获取，或者设置sdk自动获取。 </span></div><div class="line"><span class="comment">// 登录成功后，自动去取好友列表</span></div><div class="line"><span class="comment">// SDK获取结束后，会回调 </span></div><div class="line">[[EaseMob sharedInstance].chatManager asyncFetchBuddyListWithCompletion:^(NSArray *buddyList, EMError *error) &#123; &#125; onQueue:nil]<span class="string">"</span></div></pre></td></tr></table></figure>
<hr>
<h4 id="50）图文混排怎么实现？"><a href="#50）图文混排怎么实现？" class="headerlink" title="50）图文混排怎么实现？"></a>50）图文混排怎么实现？</h4><figure class="highlight cmake"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">自定义的将图片，username以及想要显示的其他内容（通过扩展属性<span class="keyword">message</span>.ext）一块显示到UI上。</div></pre></td></tr></table></figure>
<hr>
<h4 id="51）播放语音的时候，传感器打开了，但是语音播放完后，传感器还处于打开状态，什么时候关闭的？"><a href="#51）播放语音的时候，传感器打开了，但是语音播放完后，传感器还处于打开状态，什么时候关闭的？" class="headerlink" title="51）播放语音的时候，传感器打开了，但是语音播放完后，传感器还处于打开状态，什么时候关闭的？"></a>51）播放语音的时候，传感器打开了，但是语音播放完后，传感器还处于打开状态，什么时候关闭的？</h4><figure class="highlight"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">还要加上开启和关闭距离感应。</div></pre></td></tr></table></figure>
<hr>
<h4 id="52）demo总是不弹出键盘，怎么办？"><a href="#52）demo总是不弹出键盘，怎么办？" class="headerlink" title="52）demo总是不弹出键盘，怎么办？"></a>52）demo总是不弹出键盘，怎么办？</h4><figure class="highlight"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">弹出键盘的快捷键是Command + k（可以百度下xcode6的模拟器弹出键盘的方法）。</div></pre></td></tr></table></figure>
<hr>
<h4 id="53）IOS使用环信的demo进行扩展开发，上传后被拒-，说是后台播放音乐的原因，这怎么办？"><a href="#53）IOS使用环信的demo进行扩展开发，上传后被拒-，说是后台播放音乐的原因，这怎么办？" class="headerlink" title="53）IOS使用环信的demo进行扩展开发，上传后被拒 ，说是后台播放音乐的原因，这怎么办？"></a>53）IOS使用环信的demo进行扩展开发，上传后被拒 ，说是后台播放音乐的原因，这怎么办？</h4><figure class="highlight haskell"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">可以考虑去掉声音，如果要实现本地通知播放声音，</div><div class="line">再去调用localNotification的<span class="keyword">default</span> sound就可以了。</div></pre></td></tr></table></figure>
<hr>
<h4 id="54）一个群ID不能看成一个陌生人ID？"><a href="#54）一个群ID不能看成一个陌生人ID？" class="headerlink" title="54）一个群ID不能看成一个陌生人ID？"></a>54）一个群ID不能看成一个陌生人ID？</h4><figure class="highlight"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">不能。</div></pre></td></tr></table></figure>
<hr>
<h4 id="55）消息存到db里面去了"><a href="#55）消息存到db里面去了" class="headerlink" title="55）消息存到db里面去了"></a>55）消息存到db里面去了</h4><p>NSArray chats = [weakSelf.conversation loadNumbersOfMessages:(currentCount + KPageCount) before:beforeTime];<br>为什么没有拿出来呢？</p>
<figure class="highlight excel"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line">这有根据时间和数量来分别获取的。</div><div class="line">打印一下数据。有时候来新消息了，取的依然是后多少条，数据就会不对了。</div><div class="line">这个api是没问题的。<span class="built_in">db</span>可以取到说明已经存了数据。</div><div class="line">可能是跟取到的数量和时间有关系。</div></pre></td></tr></table></figure>
<hr>
<h4 id="56）cmd消息，跟聊天的消息，不存在冲突么？"><a href="#56）cmd消息，跟聊天的消息，不存在冲突么？" class="headerlink" title="56）cmd消息，跟聊天的消息，不存在冲突么？"></a>56）cmd消息，跟聊天的消息，不存在冲突么？</h4><figure class="highlight dockerfile"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">普通消息回调的方法跟这个不一样，所以不冲突，</div><div class="line">普通聊天得接收方法didReceiveMessage，</div><div class="line"><span class="keyword">cmd</span>消息的接收方法didReceiveCmdMessage。</div></pre></td></tr></table></figure>
<hr>
<h4 id="57）服务器返回登录失败，提示：logInfalse-null-error-已登录，请先调用logOff-。这是什么原因？"><a href="#57）服务器返回登录失败，提示：logInfalse-null-error-已登录，请先调用logOff-。这是什么原因？" class="headerlink" title="57）服务器返回登录失败，提示：logInfalse(null),error 已登录，请先调用logOff 。这是什么原因？"></a>57）服务器返回登录失败，提示：logInfalse(null),error 已登录，请先调用logOff 。这是什么原因？</h4><figure class="highlight"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">正在登陆的时候又发起了登陆请求。</div></pre></td></tr></table></figure>
<hr>
<h4 id="58）聊天列表是本地保存的还是从服务器上获取的？"><a href="#58）聊天列表是本地保存的还是从服务器上获取的？" class="headerlink" title="58）聊天列表是本地保存的还是从服务器上获取的？"></a>58）聊天列表是本地保存的还是从服务器上获取的？</h4><figure class="highlight awk"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">聊天列表都是保存在本地了，是sdk自动保存的。</div><div class="line">服务器上也有存聊天记录，但是客户端不能取服务器上得聊天记录。</div><div class="line">聊天记录在沙盒里，位置在<span class="regexp">/library/</span>easemobDB 里面。</div></pre></td></tr></table></figure>
<hr>
<h4 id="59）在哪修改环信logo（CFBundleDisplayName），如果是android要在哪儿改"><a href="#59）在哪修改环信logo（CFBundleDisplayName），如果是android要在哪儿改" class="headerlink" title="59）在哪修改环信logo（CFBundleDisplayName），如果是android要在哪儿改?"></a>59）在哪修改环信logo（CFBundleDisplayName），如果是android要在哪儿改?</h4><figure class="highlight stylus"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">在InfoPlist<span class="selector-class">.string</span> 文件中更改CFBundleDisplayName；</div><div class="line">android中要在SplashAcitivity里面修改。</div></pre></td></tr></table></figure>
<hr>
<h4 id="60）自动登录的回调会调用几次？"><a href="#60）自动登录的回调会调用几次？" class="headerlink" title="60）自动登录的回调会调用几次？"></a>60）自动登录的回调会调用几次？</h4><figure class="highlight"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">程序进入的时候调一次，会调用它的两个方法，</div><div class="line">willAutoLogin和didAtuoLogin。</div></pre></td></tr></table></figure>
<hr>
<h4 id="61）环信的那个-a文件里面是不是有pjsip生成的库文件呢？"><a href="#61）环信的那个-a文件里面是不是有pjsip生成的库文件呢？" class="headerlink" title="61）环信的那个.a文件里面是不是有pjsip生成的库文件呢？"></a>61）环信的那个.a文件里面是不是有pjsip生成的库文件呢？</h4><figure class="highlight"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">有的，不过还没有暴露出来。</div></pre></td></tr></table></figure>
<hr>
<h4 id="62）iOS发送消息时，收到EMErrorServerTimeout-发送消息超时的报错。-网络状况良好，可能还有其他什么原因造成？"><a href="#62）iOS发送消息时，收到EMErrorServerTimeout-发送消息超时的报错。-网络状况良好，可能还有其他什么原因造成？" class="headerlink" title="62）iOS发送消息时，收到EMErrorServerTimeout 发送消息超时的报错。 网络状况良好，可能还有其他什么原因造成？"></a>62）iOS发送消息时，收到EMErrorServerTimeout 发送消息超时的报错。 网络状况良好，可能还有其他什么原因造成？</h4><figure class="highlight"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">如果是偶尔的报错，是有可能的。</div><div class="line">因为客户端的网络本来就是一个不稳定的弱网络。</div><div class="line">用户看着感觉网络是良好的，但实际上会有各种临时性中断。</div></pre></td></tr></table></figure>
<hr>
<h4 id="63）语音信息播放以后，未读标识消失，但是关掉页面，再次进入，还有未读提示，这个是否是个bug？"><a href="#63）语音信息播放以后，未读标识消失，但是关掉页面，再次进入，还有未读提示，这个是否是个bug？" class="headerlink" title="63）语音信息播放以后，未读标识消失，但是关掉页面，再次进入，还有未读提示，这个是否是个bug？"></a>63）语音信息播放以后，未读标识消失，但是关掉页面，再次进入，还有未读提示，这个是否是个bug？</h4><figure class="highlight"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">这个是通过扩展属性来判断的，播放之后，就修改掉，</div><div class="line">再一次进来的时候，根据这个值来判断是否播放过。</div></pre></td></tr></table></figure>
<hr>
<h4 id="64）可以实现每条推送消息的昵称都自定义么？"><a href="#64）可以实现每条推送消息的昵称都自定义么？" class="headerlink" title="64）可以实现每条推送消息的昵称都自定义么？"></a>64）可以实现每条推送消息的昵称都自定义么？</h4><figure class="highlight stata"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line">环信下一步会支持自定义推送消息模板。</div><div class="line">每个<span class="keyword">app</span>可以有自己特定的模板。</div><div class="line">但按照备注来推送，这个估计支持起来是有困难的。</div><div class="line">还没想好怎么支持。</div></pre></td></tr></table></figure>
<hr>
<h4 id="65）接收本地通知的时候，通知消息前面显示的是环信id而不是nickname，怎么办？"><a href="#65）接收本地通知的时候，通知消息前面显示的是环信id而不是nickname，怎么办？" class="headerlink" title="65）接收本地通知的时候，通知消息前面显示的是环信id而不是nickname，怎么办？"></a>65）接收本地通知的时候，通知消息前面显示的是环信id而不是nickname，怎么办？</h4><figure class="highlight dos"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div></pre></td><td class="code"><pre><div class="line">"NSString <span class="built_in">title</span> = message.from;</div><div class="line"> <span class="keyword">if</span> (message.isGroup) &#123;</div><div class="line">   NSArray groupArray = [[EaseMob sharedInstance].chatManager groupList];</div><div class="line">     <span class="keyword">for</span> (EMGroup *group <span class="keyword">in</span> groupArray) &#123;</div><div class="line">       <span class="keyword">if</span> ([group.groupId isEqualToString:message.conversationChatter]) &#123; </div><div class="line">      <span class="built_in">title</span> = [NSString stringWithFormat:@""<span class="variable">%@(%</span>@)"", message.groupSenderName, group.groupSubject]; </div><div class="line">      <span class="built_in">break</span>;</div><div class="line">     &#125; </div><div class="line">  &#125; </div><div class="line">&#125; </div><div class="line">NSString <span class="built_in">title</span> = message.from中拿到这个<span class="built_in">title</span> 再去自己的服务器请求nickname，再显示到UI上。"</div></pre></td></tr></table></figure>
<hr>
<h4 id="66）-Terminating-app-due-to-uncaught-exception-‘NSInvalidArgumentException’-reason-‘-XMPPManager-networkChangedToNone-unrecognized-selector-sent-to-instance-0x15db1ef0’-然后就崩溃了？"><a href="#66）-Terminating-app-due-to-uncaught-exception-‘NSInvalidArgumentException’-reason-‘-XMPPManager-networkChangedToNone-unrecognized-selector-sent-to-instance-0x15db1ef0’-然后就崩溃了？" class="headerlink" title="66）  Terminating app due to uncaught exception ‘NSInvalidArgumentException’, reason: ‘-[XMPPManager networkChangedToNone]: unrecognized selector sent to instance 0x15db1ef0’ 然后就崩溃了？"></a>66）  Terminating app due to uncaught exception ‘NSInvalidArgumentException’, reason: ‘-[XMPPManager networkChangedToNone]: unrecognized selector sent to instance 0x15db1ef0’ 然后就崩溃了？</h4><figure class="highlight css"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">这个报错，一般是用到了环信2<span class="selector-class">.1</span><span class="selector-class">.3</span>版本里的<span class="selector-tag">SDKLite</span>包，</div><div class="line">后面的2<span class="selector-class">.1</span><span class="selector-class">.3r3</span>版本有修复，遇到类似的问题，</div><div class="line">升级到2<span class="selector-class">.1</span><span class="selector-class">.3r3</span>版本就可以解决了</div></pre></td></tr></table></figure>
<hr>
<h4 id="67）环信ios的push-inhouse类型的app支持吗"><a href="#67）环信ios的push-inhouse类型的app支持吗" class="headerlink" title="67）环信ios的push, inhouse类型的app支持吗?"></a>67）环信ios的push, inhouse类型的app支持吗?</h4><figure class="highlight"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">inhouse应该是指企业证书的，这个sdk不关注，只要推送证书对应配套就可以推送。</div></pre></td></tr></table></figure>
<hr>
<h4 id="68）点击播放语音的时候，一直显示正在下载，这个怎么解决？"><a href="#68）点击播放语音的时候，一直显示正在下载，这个怎么解决？" class="headerlink" title="68）点击播放语音的时候，一直显示正在下载，这个怎么解决？"></a>68）点击播放语音的时候，一直显示正在下载，这个怎么解决？</h4><figure class="highlight cmake"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">message</span>.downloadingAudio） 解决办法是发送方不做判断，</div><div class="line">点击之后直接播放，只在接收方做判断。</div></pre></td></tr></table></figure>
<hr>
<h4 id="69）logininfo有时候为空，是怎么回事？"><a href="#69）logininfo有时候为空，是怎么回事？" class="headerlink" title="69）logininfo有时候为空，是怎么回事？"></a>69）logininfo有时候为空，是怎么回事？</h4><figure class="highlight"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">没有登陆。</div></pre></td></tr></table></figure>
<hr>
<h4 id="70）删除好友后，好友仍然存在于好友列表中，这是为什么？"><a href="#70）删除好友后，好友仍然存在于好友列表中，这是为什么？" class="headerlink" title="70）删除好友后，好友仍然存在于好友列表中，这是为什么？"></a>70）删除好友后，好友仍然存在于好友列表中，这是为什么？</h4><figure class="highlight"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">要先清除缓存，再刷新列表。</div></pre></td></tr></table></figure>
<hr>
<h4 id="71）在登录失败的情况下，仍然进行其他操作，会出现什么提示信息？"><a href="#71）在登录失败的情况下，仍然进行其他操作，会出现什么提示信息？" class="headerlink" title="71）在登录失败的情况下，仍然进行其他操作，会出现什么提示信息？"></a>71）在登录失败的情况下，仍然进行其他操作，会出现什么提示信息？</h4><figure class="highlight"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">会提示网络错误。</div></pre></td></tr></table></figure>
<hr>
<h4 id="72）发送的消息内容在bubbleView上的哪里呀？"><a href="#72）发送的消息内容在bubbleView上的哪里呀？" class="headerlink" title="72）发送的消息内容在bubbleView上的哪里呀？"></a>72）发送的消息内容在bubbleView上的哪里呀？</h4><figure class="highlight"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">EMChatTextBubbleView里面。</div></pre></td></tr></table></figure>
<hr>
<h4 id="73）问下ios-自动重连不需要代码设置吗？"><a href="#73）问下ios-自动重连不需要代码设置吗？" class="headerlink" title="73）问下ios 自动重连不需要代码设置吗？"></a>73）问下ios 自动重连不需要代码设置吗？</h4><figure class="highlight"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">需要简单地监控一下吗？ 自动重连不需要设置。</div></pre></td></tr></table></figure>
<hr>
<h4 id="74）为什么添加好友用户搜索任何字符都可以搜索到呢？"><a href="#74）为什么添加好友用户搜索任何字符都可以搜索到呢？" class="headerlink" title="74）为什么添加好友用户搜索任何字符都可以搜索到呢？"></a>74）为什么添加好友用户搜索任何字符都可以搜索到呢？</h4><figure class="highlight maxima"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line"><span class="built_in">demo</span>没有限制，搜什么都可以搜到，开发者可以自己设定一下。</div></pre></td></tr></table></figure>
<hr>
<h4 id="75）环信是没有提供好友申请列表的接口，需要做本地数据保存是吧？"><a href="#75）环信是没有提供好友申请列表的接口，需要做本地数据保存是吧？" class="headerlink" title="75）环信是没有提供好友申请列表的接口，需要做本地数据保存是吧？"></a>75）环信是没有提供好友申请列表的接口，需要做本地数据保存是吧？</h4><figure class="highlight stata"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">好友申请未处理，重启<span class="keyword">app</span>服务器会再次给发过来。</div></pre></td></tr></table></figure>
<hr>
<h4 id="76）使用环信sdk录制的语音是什么编码格式的"><a href="#76）使用环信sdk录制的语音是什么编码格式的" class="headerlink" title="76）使用环信sdk录制的语音是什么编码格式的?"></a>76）使用环信sdk录制的语音是什么编码格式的?</h4><figure class="highlight"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">现在的版本，音频部分已经从SDK中剥离出来了，</div><div class="line">SDK已经不做处理了，全部放到UI上供开发者自行处理了。</div><div class="line">不过录的时候是wmv，转换成amr传过去，接收时再转换过来。</div></pre></td></tr></table></figure>
<hr>
<h4 id="77）消息记录保存的原理？"><a href="#77）消息记录保存的原理？" class="headerlink" title="77）消息记录保存的原理？"></a>77）消息记录保存的原理？</h4><figure class="highlight stata"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">sdk会保存所有消息，重启<span class="keyword">app</span>获取的消息记录都是在缓存中的，</div><div class="line">要调用从<span class="keyword">db</span>总获取消息记录的方法，加到缓存中，才能显示出来。</div></pre></td></tr></table></figure>
<hr>
<h4 id="78）环信有木有可以获取所有未读消息的接口？"><a href="#78）环信有木有可以获取所有未读消息的接口？" class="headerlink" title="78）环信有木有可以获取所有未读消息的接口？"></a>78）环信有木有可以获取所有未读消息的接口？</h4><figure class="highlight"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">有获取未读消息数的接口，不能获取到所有的未读消息内容。</div></pre></td></tr></table></figure>
<hr>
<h4 id="79）用户是按照环信的规范配置的，-demo在xcode5-1上面也抱错？"><a href="#79）用户是按照环信的规范配置的，-demo在xcode5-1上面也抱错？" class="headerlink" title="79）用户是按照环信的规范配置的， demo在xcode5.1上面也抱错？"></a>79）用户是按照环信的规范配置的， demo在xcode5.1上面也抱错？</h4><figure class="highlight"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">Xcode5现在不让提交项目了，必须用Xcode6运行。</div></pre></td></tr></table></figure>
<hr>
<h4 id="80）ios推送可以自己设置Apns显示内容吗？"><a href="#80）ios推送可以自己设置Apns显示内容吗？" class="headerlink" title="80）ios推送可以自己设置Apns显示内容吗？"></a>80）ios推送可以自己设置Apns显示内容吗？</h4><figure class="highlight lsl"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line">可以的，现在环信已经支持自定义推送内容了，可以支持：</div><div class="line"><span class="number">1</span>、不显示详情：“alert”：“您有一条新消息” </div><div class="line"><span class="number">2</span>、显示详情：“alert”：“ApnsName：XXXXX” </div><div class="line"><span class="number">3</span>、自定义。可以参考下环信的相关自定义推送部分的文档。</div></pre></td></tr></table></figure>
<hr>
<h4 id="81）显示详情那里，ApnsName为空时-后面的冒号能不能去掉？"><a href="#81）显示详情那里，ApnsName为空时-后面的冒号能不能去掉？" class="headerlink" title="81）显示详情那里，ApnsName为空时 后面的冒号能不能去掉？"></a>81）显示详情那里，ApnsName为空时 后面的冒号能不能去掉？</h4><figure class="highlight"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">环信目前已支持自定义推送内容。</div></pre></td></tr></table></figure>
<hr>
<h4 id="82）ios推送字符长度有没有限制"><a href="#82）ios推送字符长度有没有限制" class="headerlink" title="82）ios推送字符长度有没有限制?"></a>82）ios推送字符长度有没有限制?</h4><figure class="highlight lsl"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">跟普通消息一样，<span class="number">30</span>kb以内。另外，自定义的字段可以全部显示。</div></pre></td></tr></table></figure>
<hr>
<h4 id="83）好友申请可以发推送消息么？"><a href="#83）好友申请可以发推送消息么？" class="headerlink" title="83）好友申请可以发推送消息么？"></a>83）好友申请可以发推送消息么？</h4><figure class="highlight"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">好友申请消息不能推送，好友申请未处理，下次登录的时候服务器会发给用户。</div></pre></td></tr></table></figure>
<hr>
<h4 id="84）集成了环信最新的ios-sdk的语音通话功能-更新的时候被拒了，因为用户这边需要后台通话的功能的-，所以在工程设置中的后台打开了voico-over-ip，但是苹果不认可这样做？"><a href="#84）集成了环信最新的ios-sdk的语音通话功能-更新的时候被拒了，因为用户这边需要后台通话的功能的-，所以在工程设置中的后台打开了voico-over-ip，但是苹果不认可这样做？" class="headerlink" title="84）集成了环信最新的ios sdk的语音通话功能 更新的时候被拒了，因为用户这边需要后台通话的功能的 ，所以在工程设置中的后台打开了voico over ip，但是苹果不认可这样做？"></a>84）集成了环信最新的ios sdk的语音通话功能 更新的时候被拒了，因为用户这边需要后台通话的功能的 ，所以在工程设置中的后台打开了voico over ip，但是苹果不认可这样做？</h4><figure class="highlight"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">不用开这个。或者和苹果解释下，说用到的地方。</div></pre></td></tr></table></figure>
<hr>
<h4 id="85）做实时语音的时候调用"><a href="#85）做实时语音的时候调用" class="headerlink" title="85）做实时语音的时候调用"></a>85）做实时语音的时候调用</h4><ul>
<li>(EMCallSession <em>)asyncCallAudioWithChatter:(NSString </em>)chatter timeout:(NSUInteger)timeout error:(EMError **)pError;<br>这个函数，报这个错误<br>-[EMCallManager<br>asyncCallAudioWithChatter:timeout:error:]:<br>unrecognized selector sent to instance 0x7bea96e0（崩了）是什么原因？<figure class="highlight css"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">要是用语音实时通话， 就导<span class="selector-tag">libEaseMobClientSDK</span><span class="selector-class">.a</span>这一个库就可以了（不能导2个库）。</div></pre></td></tr></table></figure>
</li>
</ul>
<hr>
<h4 id="86）环信消息推送，是本地推送，还是远程推送？"><a href="#86）环信消息推送，是本地推送，还是远程推送？" class="headerlink" title="86）环信消息推送，是本地推送，还是远程推送？"></a>86）环信消息推送，是本地推送，还是远程推送？</h4><figure class="highlight"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">ios长连接没断开是本地通知，断开了走apns。</div></pre></td></tr></table></figure>
<hr>
<h4 id="87）iOS-文件在发送之后，-对方的接收到消息后，-对应的文件local路径是指什么？"><a href="#87）iOS-文件在发送之后，-对方的接收到消息后，-对应的文件local路径是指什么？" class="headerlink" title="87）iOS 文件在发送之后， 对方的接收到消息后， 对应的文件local路径是指什么？"></a>87）iOS 文件在发送之后， 对方的接收到消息后， 对应的文件local路径是指什么？</h4><figure class="highlight"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">这个时候文件是否已经存到对方沙盒内了吗？ 对方接受到的时缩略图，原件需要点击查看，才能下载原件。</div></pre></td></tr></table></figure>
<hr>
<h4 id="88）环信为什么没有-功能？未来有没有支持的计划"><a href="#88）环信为什么没有-功能？未来有没有支持的计划" class="headerlink" title="88）环信为什么没有@功能？未来有没有支持的计划?"></a>88）环信为什么没有@功能？未来有没有支持的计划?</h4><figure class="highlight"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">有。不过还没排期。这个肯定也要做的，是个通用需求。</div></pre></td></tr></table></figure>
<hr>
<h4 id="89）iOS环信信息下载后的文件无后缀，请问还需要什么处理吗？"><a href="#89）iOS环信信息下载后的文件无后缀，请问还需要什么处理吗？" class="headerlink" title="89）iOS环信信息下载后的文件无后缀，请问还需要什么处理吗？"></a>89）iOS环信信息下载后的文件无后缀，请问还需要什么处理吗？</h4><figure class="highlight"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">不需要，发送的是什么，下载的就是什么。</div></pre></td></tr></table></figure>
<hr>
<h4 id="90）服务端先发送的文本消息，然后ios端再发文本消息。收到的消息顺序是先收到ios端的然后在才收到服务端的。"><a href="#90）服务端先发送的文本消息，然后ios端再发文本消息。收到的消息顺序是先收到ios端的然后在才收到服务端的。" class="headerlink" title="90）服务端先发送的文本消息，然后ios端再发文本消息。收到的消息顺序是先收到ios端的然后在才收到服务端的。"></a>90）服务端先发送的文本消息，然后ios端再发文本消息。收到的消息顺序是先收到ios端的然后在才收到服务端的。</h4><figure class="highlight"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">后台发，这个应该发送效率问题，排序是按照消息接收时间。</div></pre></td></tr></table></figure>
<hr>
<h4 id="91）勾选Audio-and-AirPlay后，上传App被AppStore拒绝。"><a href="#91）勾选Audio-and-AirPlay后，上传App被AppStore拒绝。" class="headerlink" title="91）勾选Audio and AirPlay后，上传App被AppStore拒绝。"></a>91）勾选Audio and AirPlay后，上传App被AppStore拒绝。</h4><figure class="highlight"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">不勾选这个，然后在本地推送里设置本地推送提示音。</div></pre></td></tr></table></figure>
<hr>
<h4 id="92）直接通过app自己的服务器去登录环信，登录成功之后不能聊天是怎么回事？"><a href="#92）直接通过app自己的服务器去登录环信，登录成功之后不能聊天是怎么回事？" class="headerlink" title="92）直接通过app自己的服务器去登录环信，登录成功之后不能聊天是怎么回事？"></a>92）直接通过app自己的服务器去登录环信，登录成功之后不能聊天是怎么回事？</h4><figure class="highlight"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">必须通过客户端sdk登录才能聊天，客户端跟服务器之间没有长连接不能聊天。</div></pre></td></tr></table></figure>
<hr>
<h4 id="93）环信SDK哪些数据做了本地储存的？群组列表也是存了的？加好友、加入群组之类的通知呢？"><a href="#93）环信SDK哪些数据做了本地储存的？群组列表也是存了的？加好友、加入群组之类的通知呢？" class="headerlink" title="93）环信SDK哪些数据做了本地储存的？群组列表也是存了的？加好友、加入群组之类的通知呢？"></a>93）环信SDK哪些数据做了本地储存的？群组列表也是存了的？加好友、加入群组之类的通知呢？</h4><figure class="highlight vim"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">im</span>都存了，其他的都需要开发者自己处理。</div><div class="line">群组列表不是最新的，只是会报给，这个最好还是开发者自己存。</div><div class="line">加好友、加入群组之类的通知sdk不存，ui存了。</div></pre></td></tr></table></figure>
<hr>
<h4 id="94）头像在哪个类里面设置？"><a href="#94）头像在哪个类里面设置？" class="headerlink" title="94）头像在哪个类里面设置？"></a>94）头像在哪个类里面设置？</h4><figure class="highlight"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">EMChatViewBaseCell。</div></pre></td></tr></table></figure>
<hr>
<h4 id="95）IOS，只有退出登录，才算是不在线，对么？"><a href="#95）IOS，只有退出登录，才算是不在线，对么？" class="headerlink" title="95）IOS，只有退出登录，才算是不在线，对么？"></a>95）IOS，只有退出登录，才算是不在线，对么？</h4><figure class="highlight lsl"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">ios不在前台，就是不在线。</div><div class="line">但要注意进入到后台<span class="number">3</span>分钟后才不在线，</div><div class="line">因为ios系统会在进入后台<span class="number">3</span>分钟后，把应用的连接杀死。</div></pre></td></tr></table></figure>
<hr>
<h4 id="96）目前不支持群聊语音电话？"><a href="#96）目前不支持群聊语音电话？" class="headerlink" title="96）目前不支持群聊语音电话？"></a>96）目前不支持群聊语音电话？</h4><figure class="highlight"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">目前是语音单聊。</div></pre></td></tr></table></figure>
<hr>
<h4 id="97）安卓和ios-录音的格式分别是什么？"><a href="#97）安卓和ios-录音的格式分别是什么？" class="headerlink" title="97）安卓和ios 录音的格式分别是什么？"></a>97）安卓和ios 录音的格式分别是什么？</h4><figure class="highlight elixir"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div></pre></td><td class="code"><pre><div class="line">安卓是amr格式，wav格式。</div><div class="line">iOS5以前有明确的说明，不支持amr直接播放。</div><div class="line">之后就没有明确说明了。应该是也不支持。</div><div class="line">iossdk中 发送: wav-&gt;amr；  接收<span class="symbol">:amr</span> - &gt; wav，用libopencore这个库转的。</div><div class="line">另外，现在iOS端的语音部分已经全部从SDK剥离出来了，全部放到UI上供开发者自行处理。</div></pre></td></tr></table></figure>
<hr>
<h4 id="98）为什么获取不到群组的owner？"><a href="#98）为什么获取不到群组的owner？" class="headerlink" title="98）为什么获取不到群组的owner？"></a>98）为什么获取不到群组的owner？</h4><figure class="highlight sqf"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">获取与我有关的群组的时候只能看到群组ID，和群组名称，</div><div class="line">只有调用群详情的方法才能看到，调用群详情的方法，能看到描述和<span class="built_in">owner</span>。</div></pre></td></tr></table></figure>
<hr>
<h4 id="99）报错：imported-from-module-‘UIKit…’怎样改？"><a href="#99）报错：imported-from-module-‘UIKit…’怎样改？" class="headerlink" title="99）报错：imported from module ‘UIKit…’怎样改？"></a>99）报错：imported from module ‘UIKit…’怎样改？</h4><figure class="highlight elm"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">没有导入<span class="type">UIKit</span>，#<span class="keyword">import</span></div></pre></td></tr></table></figure>
<hr>
<h4 id="100）一个消息发送失败后，使用asyncSendMessage重新发送，消息的timestamp怎么更新？"><a href="#100）一个消息发送失败后，使用asyncSendMessage重新发送，消息的timestamp怎么更新？" class="headerlink" title="100）一个消息发送失败后，使用asyncSendMessage重新发送，消息的timestamp怎么更新？"></a>100）一个消息发送失败后，使用asyncSendMessage重新发送，消息的timestamp怎么更新？</h4><figure class="highlight"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">重新发送消息 ，是用的以前的时间，qq处理的也是用的以前的时间，sdk内部处理的时间。</div></pre></td></tr></table></figure>
<hr>
<h4 id="101）ios的导航栏的颜色在哪改？"><a href="#101）ios的导航栏的颜色在哪改？" class="headerlink" title="101）ios的导航栏的颜色在哪改？"></a>101）ios的导航栏的颜色在哪改？</h4><figure class="highlight clojure"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">[[UINavigationBar appearance] setBarTintColor:RGBACOLOR(<span class="number">78</span>, <span class="number">188</span>, <span class="number">211</span>, <span class="number">1</span>)]<span class="comment">;</span></div></pre></td></tr></table></figure>
<hr>
<h4 id="102）iOS要实现一个群聊不播放提示音或者震动，只能开发者自己实现？？还是像android那样有相应的接口？"><a href="#102）iOS要实现一个群聊不播放提示音或者震动，只能开发者自己实现？？还是像android那样有相应的接口？" class="headerlink" title="102）iOS要实现一个群聊不播放提示音或者震动，只能开发者自己实现？？还是像android那样有相应的接口？"></a>102）iOS要实现一个群聊不播放提示音或者震动，只能开发者自己实现？？还是像android那样有相应的接口？</h4><figure class="highlight"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">不播放，就是不调用震动和音频就行了，要自己实现。</div></pre></td></tr></table></figure>
<hr>
<h4 id="103）rest接口如果调用次数过大会报什么错？"><a href="#103）rest接口如果调用次数过大会报什么错？" class="headerlink" title="103）rest接口如果调用次数过大会报什么错？"></a>103）rest接口如果调用次数过大会报什么错？</h4><figure class="highlight lsl"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">会返回：<span class="number">503</span> （服务不可用） 服务器目前无法使用（由于超载或停机维护）。 通常，这只是暂时状态。</div></pre></td></tr></table></figure>
<hr>
<h4 id="104）-self-setupUIDemoDB-这个是必须的吗"><a href="#104）-self-setupUIDemoDB-这个是必须的吗" class="headerlink" title="104）[self setupUIDemoDB] 这个是必须的吗?"></a>104）[self setupUIDemoDB] 这个是必须的吗?</h4><figure class="highlight excel"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">不是。这个是demo中用到的第三方<span class="built_in">DB</span>，存的是好友申请这一类的消息。</div><div class="line">不建议使用，可以自己写一个<span class="built_in">DB</span>进行存储</div></pre></td></tr></table></figure>
<hr>
<h4 id="105）在模拟器上登陆的时候会报这个错误，是怎么回事？"><a href="#105）在模拟器上登陆的时候会报这个错误，是怎么回事？" class="headerlink" title="105）在模拟器上登陆的时候会报这个错误，是怎么回事？"></a>105）在模拟器上登陆的时候会报这个错误，是怎么回事？</h4><figure class="highlight"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">应该是用户名多敲了空格。</div></pre></td></tr></table></figure>
<hr>
<h4 id="105）两个客户端，但是却是不同的应用，为了实现两者的通信，用了第一个的appkey-用户也都是在第一个app下注册的，但是由于推送证书要绑定app-所以第二个没有推送，请问怎么解决？"><a href="#105）两个客户端，但是却是不同的应用，为了实现两者的通信，用了第一个的appkey-用户也都是在第一个app下注册的，但是由于推送证书要绑定app-所以第二个没有推送，请问怎么解决？" class="headerlink" title="105）两个客户端，但是却是不同的应用，为了实现两者的通信，用了第一个的appkey,用户也都是在第一个app下注册的，但是由于推送证书要绑定app,所以第二个没有推送，请问怎么解决？"></a>105）两个客户端，但是却是不同的应用，为了实现两者的通信，用了第一个的appkey,用户也都是在第一个app下注册的，但是由于推送证书要绑定app,所以第二个没有推送，请问怎么解决？</h4><figure class="highlight"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">可以上传多套证书，两个应用之间通信只要保证是同一个appkey 就行， </div><div class="line">如果是推送的话 ，要保证项目中的证书名称跟上传到后台的证书名称一致。</div></pre></td></tr></table></figure>
<hr>
<h4 id="106）IOS删除所有聊天记录的方法是哪个？"><a href="#106）IOS删除所有聊天记录的方法是哪个？" class="headerlink" title="106）IOS删除所有聊天记录的方法是哪个？"></a>106）IOS删除所有聊天记录的方法是哪个？</h4><figure class="highlight"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">又是iOS代码，烦不烦？？？</div></pre></td></tr></table></figure>
<hr>
<h4 id="107）用户发过来的语音消息-这边听过后-黄点（未读标识）不消失，-是怎么回事？"><a href="#107）用户发过来的语音消息-这边听过后-黄点（未读标识）不消失，-是怎么回事？" class="headerlink" title="107）用户发过来的语音消息,这边听过后,黄点（未读标识）不消失， 是怎么回事？"></a>107）用户发过来的语音消息,这边听过后,黄点（未读标识）不消失， 是怎么回事？</h4><figure class="highlight less"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line">全局搜一下prepareMessageAudioModel这个函数 </div><div class="line">还有modelWithMessage，</div><div class="line">把 <span class="selector-attr">[[EaseMob sharedInstance]</span><span class="selector-class">.chatManager</span> <span class="selector-tag">insertMessageToDB</span><span class="selector-pseudo">:chatMessage</span>]; </div><div class="line">换成<span class="selector-attr">[chatMessage?updateMessageExtToDB]</span>;</div></pre></td></tr></table></figure>
<hr>
<h4 id="108）-NSConcreteData-AES256Decrypt-unrecognized-selector-sent-to-instance-XXX，怎么办？"><a href="#108）-NSConcreteData-AES256Decrypt-unrecognized-selector-sent-to-instance-XXX，怎么办？" class="headerlink" title="108）[NSConcreteData AES256Decrypt]: unrecognized selector sent to instance XXX，怎么办？"></a>108）[NSConcreteData AES256Decrypt]: unrecognized selector sent to instance XXX，怎么办？</h4><figure class="highlight lasso"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">other <span class="keyword">link</span> flags 设置<span class="params">-ObjC</span> 注意大小写。</div></pre></td></tr></table></figure>
<hr>
<h4 id="109）framework-not-found-Metal-for-architecture-armv7s"><a href="#109）framework-not-found-Metal-for-architecture-armv7s" class="headerlink" title="109）framework not found Metal for architecture armv7s"></a>109）framework not found Metal for architecture armv7s</h4><figure class="highlight"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">需要升级到xcode6。</div></pre></td></tr></table></figure>
<hr>
<h4 id="110）SDK太大了，怎么办？"><a href="#110）SDK太大了，怎么办？" class="headerlink" title="110）SDK太大了，怎么办？"></a>110）SDK太大了，怎么办？</h4><figure class="highlight lsl"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">SDK大不影响打包成ipa，打包出ipa安装包后，一般只会增加<span class="number">2</span>MB左右。</div></pre></td></tr></table></figure>
<hr>
<h4 id="111）SDK是否支持64位？"><a href="#111）SDK是否支持64位？" class="headerlink" title="111）SDK是否支持64位？"></a>111）SDK是否支持64位？</h4><figure class="highlight"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">支持。</div></pre></td></tr></table></figure>
<hr>
<h4 id="112）无法收到聊天消息？"><a href="#112）无法收到聊天消息？" class="headerlink" title="112）无法收到聊天消息？"></a>112）无法收到聊天消息？</h4><figure class="highlight less"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div></pre></td><td class="code"><pre><div class="line">接收聊天等回调，要注意以下几步骤：</div><div class="line"> 1、声明实现IChatManagerDelegate，如：</div><div class="line">   <span class="variable">@interface</span> EaseMobSDKAccountManager ()</div><div class="line"> <span class="number">2</span>、注册当前类接收回调 [[EaseMob sharedInstance].chatManager <span class="attribute">addDelegate</span>:self <span class="attribute">delegateQueue</span>:nil];</div><div class="line"> <span class="selector-tag">3</span>、实现对应回调方法，如：</div><div class="line"> <span class="comment">// 收消息的回调*x000D*</span></div><div class="line">   <span class="selector-tag">-</span>(void)<span class="selector-tag">didReceiveMessage</span><span class="selector-pseudo">:(EMMessage</span> )<span class="selector-tag">message</span>&#123;x000D__x000D&#125;</div></pre></td></tr></table></figure>
<hr>
<h4 id="113）一条消息收到多次，为什么？"><a href="#113）一条消息收到多次，为什么？" class="headerlink" title="113）一条消息收到多次，为什么？"></a>113）一条消息收到多次，为什么？</h4><figure class="highlight"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">一般是由本类多次注册了回调导致，请确定以下方法只执行了一次</div></pre></td></tr></table></figure>
<hr>
<h4 id="114）无法收到好友请求，怎么办？"><a href="#114）无法收到好友请求，怎么办？" class="headerlink" title="114）无法收到好友请求，怎么办？"></a>114）无法收到好友请求，怎么办？</h4><figure class="highlight"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">请确定接受好友请求的类是否初始化，</div><div class="line">并声明了IChatManagerDelegate，实现了注册回调和回调方法。</div></pre></td></tr></table></figure>
<hr>
<h4 id="115）APP后台时无法收到推送，怎么办？"><a href="#115）APP后台时无法收到推送，怎么办？" class="headerlink" title="115）APP后台时无法收到推送，怎么办？"></a>115）APP后台时无法收到推送，怎么办？</h4><figure class="highlight mipsasm"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">环信将notification分为两种，一种是本地通知，一种是apns。</div><div class="line">当您后台时，长连接还存在，会走<span class="keyword">didReceiveMessage方法，</span></div><div class="line">需要您自己维护本地通知（即LocalNotification）。</div></pre></td></tr></table></figure>
<hr>
<h4 id="116）APP彻底关闭，无法收到推送？"><a href="#116）APP彻底关闭，无法收到推送？" class="headerlink" title="116）APP彻底关闭，无法收到推送？"></a>116）APP彻底关闭，无法收到推送？</h4><figure class="highlight lsl"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div></pre></td><td class="code"><pre><div class="line">用户可以通过以下几步检验自己的推送</div><div class="line"><span class="number">1.</span>首先需要用户上传APNS推送证书到环信管理后台。</div><div class="line"><span class="number">2.</span>在环信iOSSDK初始化时填写用户自己的证书名称。</div><div class="line"><span class="number">3.</span>注册推送。</div><div class="line"><span class="number">4.</span>真机登录环信im账号。</div><div class="line"><span class="number">5.</span>查看管理后台中，对应im账户下是否有用户刚刚写的证书名。(如果没有，请检查是否得到了deviceToken）</div><div class="line"><span class="number">6.</span>确定用户当前证书是否和自己的项目匹配。（开发证书与生产证书需要一一对应</div></pre></td></tr></table></figure>
<hr>
<h4 id="117）环信-pod-上面的SDK是最新么？"><a href="#117）环信-pod-上面的SDK是最新么？" class="headerlink" title="117）环信 pod 上面的SDK是最新么？"></a>117）环信 pod 上面的SDK是最新么？</h4><figure class="highlight markdown"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div></pre></td><td class="code"><pre><div class="line">通过Cocoapods下载地址 </div><div class="line">不包含实时语音版本SDK（EaseMobClientSDKLite），</div><div class="line">引用时 #importpod'EaseMobSDK', :git =&gt; '[<span class="string">https://github.com/easemob/sdk-ios-cocoapods.git</span>](<span class="link">https://github.com/easemob/sdk-ios-cocoapods.git</span>)'</div><div class="line"> 包含实时语音版本SDK（EaseMobClientSDK），</div><div class="line">引用时 #importpod 'EaseMobSDKFull', :git =&gt; '[<span class="string">https://github.com/easemob/sdk-ios-cocoapods-integration.git</span>](<span class="link">https://github.com/easemob/sdk-ios-cocoapods-integration.git</span>)'</div></pre></td></tr></table></figure>
<hr>
<h4 id="118）注册deviceToken回调出现error，注册推送失败errorCode-3000"><a href="#118）注册deviceToken回调出现error，注册推送失败errorCode-3000" class="headerlink" title="118）注册deviceToken回调出现error，注册推送失败errorCode = 3000?"></a>118）注册deviceToken回调出现error，注册推送失败errorCode = 3000?</h4><figure class="highlight awk"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div></pre></td><td class="code"><pre><div class="line">这是配置有问题，还没有到环信。</div><div class="line">一般是profile文件问题，或者证书没有打开推送功能。 </div><div class="line">类似这个时候报的errorCode，可以百度解决，</div><div class="line">[http:<span class="regexp">//</span>www.educity.cn<span class="regexp">/wenda/</span><span class="number">100742</span>.html。]</div><div class="line">(http:<span class="regexp">//</span>www.educity.cn<span class="regexp">/wenda/</span><span class="number">100742</span>.html%E3%<span class="number">80</span>%<span class="number">82</span>)</div></pre></td></tr></table></figure>
<hr>
<h4 id="119）为什么设置EMPushNotificationOptions返回error-是nil，设置也不对？"><a href="#119）为什么设置EMPushNotificationOptions返回error-是nil，设置也不对？" class="headerlink" title="119）为什么设置EMPushNotificationOptions返回error 是nil，设置也不对？"></a>119）为什么设置EMPushNotificationOptions返回error 是nil，设置也不对？</h4><figure class="highlight"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">EMPushNotificationOptions设置需要在您获取deviceToken成功后才能设置。请先确定deviceToken获取没问题。</div></pre></td></tr></table></figure>
<hr>
<h4 id="120）为什么提交的APNS昵称和使用证书名后台看不到"><a href="#120）为什么提交的APNS昵称和使用证书名后台看不到" class="headerlink" title="120）为什么提交的APNS昵称和使用证书名后台看不到?"></a>120）为什么提交的APNS昵称和使用证书名后台看不到?</h4><figure class="highlight"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">这两个也是需要用户确定了deviceToken正确获取后才可以。</div></pre></td></tr></table></figure>
<hr>
<h4 id="121）项目中是否可以用其他推送，和环信推送是否有冲突？"><a href="#121）项目中是否可以用其他推送，和环信推送是否有冲突？" class="headerlink" title="121）项目中是否可以用其他推送，和环信推送是否有冲突？"></a>121）项目中是否可以用其他推送，和环信推送是否有冲突？</h4><figure class="highlight"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">没有冲突。</div></pre></td></tr></table></figure>
<hr>
<h4 id="122）是不是可以用环信推广告之类的其他信息？"><a href="#122）是不是可以用环信推广告之类的其他信息？" class="headerlink" title="122）是不是可以用环信推广告之类的其他信息？"></a>122）是不是可以用环信推广告之类的其他信息？</h4><figure class="highlight"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">环信的推送主要为IM服务，如果有这些需求，建议开发者用专用的推送服务。</div></pre></td></tr></table></figure>
<hr>
<h4 id="123）后台上传证书显示异常，怎么办？"><a href="#123）后台上传证书显示异常，怎么办？" class="headerlink" title="123）后台上传证书显示异常，怎么办？"></a>123）后台上传证书显示异常，怎么办？</h4><figure class="highlight"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">异常一般是因为证书制作过程不对，或者密码输入有问题，</div><div class="line">密码为开发者导出证书时设置的密码。</div></pre></td></tr></table></figure>
<hr>
<h4 id="124）证书不设置密码是否可用？"><a href="#124）证书不设置密码是否可用？" class="headerlink" title="124）证书不设置密码是否可用？"></a>124）证书不设置密码是否可用？</h4><figure class="highlight"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">目前要求必须设置密码。</div></pre></td></tr></table></figure>
<hr>
<h4 id="125）项目要提交了，想测下，如何操作？"><a href="#125）项目要提交了，想测下，如何操作？" class="headerlink" title="125）项目要提交了，想测下，如何操作？"></a>125）项目要提交了，想测下，如何操作？</h4><figure class="highlight arduino"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">打包时使用ad-hoc模式，可以打包出一个<span class="built_in">release</span>版本的ipa，用这个测试即可。</div></pre></td></tr></table></figure>
<hr>
<h4 id="126）如果开发者想按home键后立刻收到推送，如何处理？"><a href="#126）如果开发者想按home键后立刻收到推送，如何处理？" class="headerlink" title="126）如果开发者想按home键后立刻收到推送，如何处理？"></a>126）如果开发者想按home键后立刻收到推送，如何处理？</h4><figure class="highlight bash"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">因为这个时候用户还是长连接中，</div><div class="line">可以监听didReceiveMessage方法，</div><div class="line">之后开发者自己实现<span class="built_in">local</span>Notification。</div></pre></td></tr></table></figure>
<hr>
<h4 id="127）IOS做退出登录有请求超时的问题。"><a href="#127）IOS做退出登录有请求超时的问题。" class="headerlink" title="127）IOS做退出登录有请求超时的问题。"></a>127）IOS做退出登录有请求超时的问题。</h4><figure class="highlight gams"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">退出登陆时，真机时把isUnbind这个属性设为<span class="keyword">YES</span>， </div><div class="line">解除device_token绑定，如果出现退出解绑失败的情况，</div><div class="line">可能是没有网络。</div></pre></td></tr></table></figure>
<hr>
<h4 id="128）群消息ios登录用户发群消息，android的登录用户收不到是什么原因？"><a href="#128）群消息ios登录用户发群消息，android的登录用户收不到是什么原因？" class="headerlink" title="128）群消息ios登录用户发群消息，android的登录用户收不到是什么原因？"></a>128）群消息ios登录用户发群消息，android的登录用户收不到是什么原因？</h4><figure class="highlight stata"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">处理方法：请提供下发送双方的<span class="keyword">log</span>日志，和这个<span class="keyword">app</span>的appkey，<span class="keyword">token</span>，以及丢得消息的id，工作人员会进行排查。</div></pre></td></tr></table></figure>
<hr>
<h4 id="129）把appkey下的所有用户全部设置成免打扰。有相关接口么？"><a href="#129）把appkey下的所有用户全部设置成免打扰。有相关接口么？" class="headerlink" title="129）把appkey下的所有用户全部设置成免打扰。有相关接口么？"></a>129）把appkey下的所有用户全部设置成免打扰。有相关接口么？</h4><figure class="highlight"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">不设置推送证书，如果设置了，删掉即可。</div></pre></td></tr></table></figure>
<hr>
<h4 id="130）环信的SDK在xcode7上能用么？"><a href="#130）环信的SDK在xcode7上能用么？" class="headerlink" title="130）环信的SDK在xcode7上能用么？"></a>130）环信的SDK在xcode7上能用么？</h4><figure class="highlight css"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">目前的最新版本2<span class="selector-class">.2</span><span class="selector-class">.0</span>已经支持。</div></pre></td></tr></table></figure>
<hr>
<h4 id="131）自动登陆的状态什么时候设置为no，只有退出登陆才设置吗？"><a href="#131）自动登陆的状态什么时候设置为no，只有退出登陆才设置吗？" class="headerlink" title="131）自动登陆的状态什么时候设置为no，只有退出登陆才设置吗？"></a>131）自动登陆的状态什么时候设置为no，只有退出登陆才设置吗？</h4><figure class="highlight coffeescript"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">退出的时候自动就变成<span class="literal">no</span>了。</div></pre></td></tr></table></figure>
<hr>
<h4 id="132）聊天室中头像和昵称的获取，现在有什么方案吗？"><a href="#132）聊天室中头像和昵称的获取，现在有什么方案吗？" class="headerlink" title="132）聊天室中头像和昵称的获取，现在有什么方案吗？"></a>132）聊天室中头像和昵称的获取，现在有什么方案吗？</h4><figure class="highlight markdown"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">和群聊的头像昵称方案是一样的，可以参考：</div><div class="line">  [<span class="string">http://docs.easemob.com/doku.php?id=start:000quickstart:40nickna</span>](<span class="link">http://docs.easemob.com/doku.php?id=start:000quickstart:40nickna</span>)</div></pre></td></tr></table></figure>
<hr>
<h4 id="133）1-2-1-9版本的sdk-a包会有与fmpeg-冲突的情况，这个要怎么处理呀？2-他们用到了这个http-videojj-com-视频播放的第三方？"><a href="#133）1-2-1-9版本的sdk-a包会有与fmpeg-冲突的情况，这个要怎么处理呀？2-他们用到了这个http-videojj-com-视频播放的第三方？" class="headerlink" title="133）1. 2.1.9版本的sdk.a包会有与fmpeg 冲突的情况，这个要怎么处理呀？2.他们用到了这个http://videojj.com/,视频播放的第三方？"></a>133）1. 2.1.9版本的sdk.a包会有与fmpeg 冲突的情况，这个要怎么处理呀？2.他们用到了这个<a href="http://videojj.com/,%E8%A7%86%E9%A2%91%E6%92%AD%E6%94%BE%E7%9A%84%E7%AC%AC%E4%B8%89%E6%96%B9%EF%BC%9F" target="_blank" rel="external">http://videojj.com/,视频播放的第三方？</a></h4><figure class="highlight"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">如果用到了环信的实时语音库，同时也用到了其它第三方的库，就会冲突。</div></pre></td></tr></table></figure>
<hr>
<h4 id="134）发实时语音-iOS-不走apns"><a href="#134）发实时语音-iOS-不走apns" class="headerlink" title="134）发实时语音 iOS 不走apns?"></a>134）发实时语音 iOS 不走apns?</h4><figure class="highlight arduino"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">目前还不支持，现在实时语音是通过P2P直连或者<span class="built_in">Server</span> relay。</div></pre></td></tr></table></figure>
<hr>
]]></content>
    </entry>
    
    <entry>
      <title><![CDATA[Android使用tint一张图制作selector]]></title>
      <url>http://blog.prozin.xyz/2016/09/17/%E4%BD%BF%E7%94%A8tint%E5%88%B6%E4%BD%9Cselector/</url>
      <content type="html"><![CDATA[<p>Android 有时候制作按下的效果挺麻烦的，得放色值不同的两张图，这个就比较尴尬了，明明是相同的资源。现在Android Material Design 中提供了一个东西：Tint，一张矢量图是能适配所有的颜色。</p>
<p>tip:内有音乐自动播放。</p>
<a id="more"></a>
<iframe frameborder="no" border="0" marginwidth="0" marginheight="0" width="330" height="86" src="http://music.163.com/outchain/player?type=2&id=35476049&auto=0&height=66"></iframe>

<hr>
<h1 id="首先这个东西可以直接对-ImageView-上色渲染"><a href="#首先这个东西可以直接对-ImageView-上色渲染" class="headerlink" title="首先这个东西可以直接对 ImageView 上色渲染"></a>首先这个东西可以直接对 ImageView 上色渲染</h1><p>如下，相同的两个图，使用 tint 改变颜色:</p>
<pre><code>&lt;ImageView
    android:layout_width=&quot;wrap_content&quot;
    android:layout_height=&quot;wrap_content&quot;
    android:layout_margin=&quot;10dp&quot;
    android:src=&quot;@mipmap/ic_launcher&quot; /&gt;

&lt;ImageView
    android:layout_width=&quot;wrap_content&quot;
    android:layout_height=&quot;wrap_content&quot;
    android:layout_margin=&quot;10dp&quot;
    android:src=&quot;@mipmap/ic_launcher&quot;
    android:tint=&quot;#FFF&quot; /&gt;
</code></pre><h1 id="使用-tint-制作selector"><a href="#使用-tint-制作selector" class="headerlink" title="使用 tint 制作selector"></a>使用 tint 制作selector</h1><h2 id="以下为成品的-selector，selector-bg-xml"><a href="#以下为成品的-selector，selector-bg-xml" class="headerlink" title="以下为成品的 selector，selector_bg.xml:"></a>以下为成品的 selector，selector_bg.xml:</h2><pre><code>&lt;?xml version=&quot;1.0&quot; encoding=&quot;utf-8&quot;?&gt;
&lt;selector xmlns:android=&quot;http://schemas.android.com/    apk/res/android&quot;&gt;
    &lt;item android:drawable=&quot;@drawable/btn_begin_pressed&quot; android:state_pressed=&quot;true&quot;&gt;
    &lt;/item&gt;
    &lt;item android:drawable=&quot;@drawable/btn_begin&quot;&gt;
    &lt;/item&gt;
&lt;/selector&gt;
</code></pre><p>其中btn_begin为png图片，而btn_begin_pressed则是使用此图进行tint着色后的xml文件.</p>
<h2 id="btn-begin-pressed-xml"><a href="#btn-begin-pressed-xml" class="headerlink" title="btn_begin_pressed.xml:"></a>btn_begin_pressed.xml:</h2><pre><code>&lt;?xml version=&quot;1.0&quot; encoding=&quot;utf-8&quot;?&gt;
&lt;bitmap xmlns:android=&quot;http://schemas.android.com/apk/res/android&quot;
    android:src=&quot;@drawable/btn_begin_down&quot;
    android:tint=&quot;#7b5b5b&quot;
    android:tintMode=&quot;multiply&quot;&gt;
&lt;/bitmap&gt;
</code></pre><p>android:tint： 设置的是颜色</p>
<p>android:tintMode：设置的是类型(src_in,src_over,src_atop,add,screen,multiply)</p>
<p>各个属性效果如图：</p>
<p><img src="http://img.blog.csdn.net/20150814145505737" alt="tinMode"></p>
<p><strong>然后不管在xml布局中android:background，还是java代码setBackgroundResource，都可以直接使用成品背景图了</strong></p>
<h1 id="改变MD下的-EditText-背景色和光标颜色"><a href="#改变MD下的-EditText-背景色和光标颜色" class="headerlink" title="改变MD下的 EditText 背景色和光标颜色"></a>改变MD下的 EditText 背景色和光标颜色</h1><p>为了实现 Material Design的效果，使用的主题里的颜色配置比如 primaryColor，colorControlNormal，colorControlActived什么的会让 EditText 自动适配背景颜色，<br>其实现方式就是使用了 tint ,如下图:</p>
<p><img src="http://upload-images.jianshu.io/upload_images/1916953-d516fd40ac84f38f.gif?imageMogr2/auto-orient/strip&amp;_=1984448366" alt="随便举个栗子"></p>
<p>先不用在意那个 TextInputLayout,看背景色，对就是那条线./抠鼻~<br>然后我们不想用这个颜色怎么办捏？</p>
<p>对于那条线可以直接设置:</p>
<pre><code>android:backgroundTint=&quot;@color/颜色&quot;
</code></pre><p>额，那个光标也想改了？？需要新建一个 cursor_shape.xml:</p>
<pre><code>&lt;?xml version=&quot;1.0&quot; encoding=&quot;utf-8&quot;?&gt;
&lt;shape xmlns:android=&quot;http://schemas.android.com/apk/res/android&quot; &gt;
        &lt;size android:width=&quot;1dp&quot; /&gt;
    &lt;solid android:color=&quot;@color/颜色&quot; /&gt;
&lt;/shape&gt;
</code></pre><p>然后就可以设置:</p>
<pre><code>android:textCursorDrawable=&quot;@drawable/cursor_shape&quot;
</code></pre><h1 id="关于-tint-的源码分析参考："><a href="#关于-tint-的源码分析参考：" class="headerlink" title="关于 tint 的源码分析参考："></a>关于 tint 的源码分析参考：</h1><p><a href="https://segmentfault.com/a/1190000003038675" target="_blank" rel="external">谈谈Android Material Design 中的Tint（着色）</a></p>
<h1 id="再说点儿"><a href="#再说点儿" class="headerlink" title="再说点儿"></a>再说点儿</h1><p>说到了 tint 着色，不得不提一下Android5.0加入的取色器：Palette,可以根据Bitmap显示出来的东西提取颜色，然后到处 set，达到背景或者 toolBar 和图片达到和谐，看看效果：</p>
<p><img src="http://img.blog.csdn.net/20141122233658247" alt="MD大法好"></p>
<p>好吧随便盗的图，就提一下这个东西，还没有 Demo…</p>
<hr>
<p>本文来自：<a href="http://www.paradoxie.cf/" target="_blank" rel="external">谢盒盒的小黑屋</a>，感谢阅读(<em>^__^</em>)~</p>
]]></content>
    </entry>
    
    <entry>
      <title><![CDATA[博客需要完善的]]></title>
      <url>http://blog.prozin.xyz/2016/09/17/%E5%8D%9A%E5%AE%A2%E9%9C%80%E8%A6%81%E5%AE%8C%E5%96%84%E7%9A%84/</url>
      <content type="html"><![CDATA[<p>一边装饰环境，一边发布博客咯，记录一些开发中遇到的可以模块化的东西，管它简不简单容不容易实现呢~<br><a id="more"></a></p>
<h1 id="1-关于页面"><a href="#1-关于页面" class="headerlink" title="1.关于页面"></a>1.关于页面</h1><h1 id="2-最近访客"><a href="#2-最近访客" class="headerlink" title="2.最近访客"></a>2.最近访客</h1><h1 id="3-尝试图标替换"><a href="#3-尝试图标替换" class="headerlink" title="3.尝试图标替换"></a>3.尝试图标替换</h1><h1 id="4-把搜索弄到主页常驻"><a href="#4-把搜索弄到主页常驻" class="headerlink" title="4.把搜索弄到主页常驻"></a>4.把搜索弄到主页常驻</h1><h1 id="5-访客和总点击数量的文字描述"><a href="#5-访客和总点击数量的文字描述" class="headerlink" title="5.访客和总点击数量的文字描述"></a>5.访客和总点击数量的文字描述</h1><h1 id="6-我的更多资料"><a href="#6-我的更多资料" class="headerlink" title="6.我的更多资料"></a>6.我的更多资料</h1><h1 id="7-menu页更多分区"><a href="#7-menu页更多分区" class="headerlink" title="7.menu页更多分区"></a>7.menu页更多分区</h1><h1 id="8-插入视频和音乐"><a href="#8-插入视频和音乐" class="headerlink" title="8.插入视频和音乐"></a>8.插入视频和音乐</h1><h1 id="9-图床"><a href="#9-图床" class="headerlink" title="9.图床"></a>9.图床</h1>]]></content>
    </entry>
    
    <entry>
      <title><![CDATA[Mac OS使用github+hexo搭建自己的博客]]></title>
      <url>http://blog.prozin.xyz/2016/09/16/macos%E4%BD%BF%E7%94%A8github+hexo%E6%90%AD%E5%BB%BA%E8%87%AA%E5%B7%B1%E7%9A%84%E5%8D%9A%E5%AE%A2/</url>
      <content type="html"><![CDATA[<p>虽然这样的博客已经很多的，但是自己在搭建的事后还是遇到了不少问题，而且是在我看了好几篇文章的情况下。<br>我觉得主要还是不熟悉，但是谁一开始就会对自己没做过的事情熟悉呢？</p>
<p>所以我写这篇博客记录一下自己的搭建过程，自己博客的第一篇内容，也算是帮助想要搭建博客的人多一条参考。</p>
<a id="more"></a>
<iframe frameborder="no" border="0" marginwidth="0" marginheight="0" width="330" height="86" src="http://music.163.com/outchain/player?type=2&id=38576323&auto=1&height=66"></iframe>


<hr>
<p>本次搭建主要是参考了以下两篇文章：</p>
<p>1.<a href="http://www.jianshu.com/p/701b1095da11" target="_blank" rel="external">手把手教你建github技术博客</a></p>
<p>2.<a href="http://www.jianshu.com/p/0823e387c019" target="_blank" rel="external">CentOS下搭建Hexo + github 博客</a></p>
<p>所以能看到这篇博客的朋友，可以根据上面两篇对照着看，确保自己踩坑量达到最少。</p>
<hr>
<h2 id="好，开始"><a href="#好，开始" class="headerlink" title="好，开始"></a>好，开始</h2><p>先讲讲思路：</p>
<p>①. 本地在一个特定的环境(hexo文件夹)中写文章(语法markdown)。</p>
<p>②. 利用命令行把项目『编译』成静态网页(node.js)的形式，达到本地可访问的目的。</p>
<p>③. 在github新建一个仓库，命名要求：你的github用户名.github.io。(注：用户名就是你新建一个仓库过后，项目名的【/】前面那个)</p>
<p>④. 配置自己生成的SSH密钥到github，达到链接github的作用。</p>
<p>⑤. 配置hexo里面的_config.yml文件，这里面倒数第二行的repo: 关键字，链接第三步的仓库用，值为仓库地址。</p>
<p>⑥. 然后就是hexo的几个常用命令的用法了，新建-&gt;编辑—&gt;部署。</p>
<pre><code>新建文章：hexo new &quot;标题&quot;

发布之前清除缓存：hexo clean

生成静态页面：hexo generate

本地预览，localhost:4000可访问看看效果：hexo server #&apos;Ctrl+C&apos;关闭

部署到github，外网可访问：hexo deploy

以下信息表示部署成功即可访问：[info] Deploy done: git
</code></pre><hr>
<h3 id="1-前提准备工作"><a href="#1-前提准备工作" class="headerlink" title="1.前提准备工作"></a>1.前提准备工作</h3><p>①. 有自己的git环境，没有的去安装，环境变量什么的该弄好弄好。</p>
<p>②. 熟悉github的套路，至少会新建项目以及clone过别人的项目。</p>
<p>③. 会用MarkDown语法，可以不用熟悉，写博客的时候临时百度也是可以的。</p>
<p>④. 安装<a href="https://nodejs.org/en/" target="_blank" rel="external">Node.js</a>环境，一路安装就行了，静态网页依赖于这个。</p>
<h3 id="2-安装hexo"><a href="#2-安装hexo" class="headerlink" title="2.安装hexo"></a>2.安装hexo</h3><p>①. 命令行输入：</p>
<figure class="highlight cmake"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">npm <span class="keyword">install</span> -g hexo</div></pre></td></tr></table></figure>
<p>②. 在想要存放博客文章的位置新建一个hexo文件夹，比如我直接在Documents文件夹新建的。</p>
<p>③. 命令行切换到hexo路径下，执行以下命令：</p>
<figure class="highlight ebnf"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line"><span class="attribute">hexo init</span></div></pre></td></tr></table></figure>
<p>安装依赖包</p>
<figure class="highlight cmake"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">npm <span class="keyword">install</span></div></pre></td></tr></table></figure>
<h4 id="这样本地的事情就算完了，利用本地地址应该可以访问到了。接下来是如何跟github街上关系，使得大家都能通过互联网访问。"><a href="#这样本地的事情就算完了，利用本地地址应该可以访问到了。接下来是如何跟github街上关系，使得大家都能通过互联网访问。" class="headerlink" title="这样本地的事情就算完了，利用本地地址应该可以访问到了。接下来是如何跟github街上关系，使得大家都能通过互联网访问。"></a>这样本地的事情就算完了，利用本地地址应该可以访问到了。接下来是如何跟github街上关系，使得大家都能通过互联网访问。</h4><h3 id="3-连接github"><a href="#3-连接github" class="headerlink" title="3.连接github"></a>3.连接github</h3><p>①.    新建仓库，仓库名需要和你的账号对应，格式：yourname.github.io，拿我的举例：paradoxie.github.io</p>
<p>②. 本地生成SSH密钥，命令行：</p>
<figure class="highlight css"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line"><span class="selector-tag">ssh-keygen</span> <span class="selector-tag">-t</span> <span class="selector-tag">rsa</span> <span class="selector-tag">-C</span> <span class="selector-tag">paradoxieix</span>@<span class="keyword">gmail</span>.<span class="keyword">com</span></div></pre></td></tr></table></figure>
<p>主要C后面的邮箱地址替换成自己的。然后应该回让设置密码，可以全部为空。然后我在我的/Users/用户名/.ssh路径下看到了两个新生成的东西：id_rsa和id_rsa.pub，我们需要复制.pub里面的全部内容，最好用sublime打开。</p>
<p>③. 打开<a href="https://github.com/settings/ssh，点击New" target="_blank" rel="external">https://github.com/settings/ssh，点击New</a> SSH key,然后粘贴.pub里面的全部内容进去，然后Add SSH key.</p>
<p>④.然后在hexo文件夹内找到_config.yml配置文件，sublime打开编辑</p>
<figure class="highlight yaml"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div><div class="line">17</div><div class="line">18</div><div class="line">19</div><div class="line">20</div><div class="line">21</div><div class="line">22</div><div class="line">23</div><div class="line">24</div><div class="line">25</div><div class="line">26</div><div class="line">27</div><div class="line">28</div><div class="line">29</div><div class="line">30</div><div class="line">31</div><div class="line">32</div><div class="line">33</div><div class="line">34</div><div class="line">35</div><div class="line">36</div><div class="line">37</div><div class="line">38</div><div class="line">39</div><div class="line">40</div><div class="line">41</div><div class="line">42</div><div class="line">43</div><div class="line">44</div><div class="line">45</div><div class="line">46</div><div class="line">47</div><div class="line">48</div><div class="line">49</div><div class="line">50</div><div class="line">51</div><div class="line">52</div><div class="line">53</div><div class="line">54</div><div class="line">55</div><div class="line">56</div><div class="line">57</div><div class="line">58</div><div class="line">59</div><div class="line">60</div><div class="line">61</div><div class="line">62</div><div class="line">63</div><div class="line">64</div><div class="line">65</div><div class="line">66</div><div class="line">67</div><div class="line">68</div><div class="line">69</div><div class="line">70</div><div class="line">71</div><div class="line">72</div><div class="line">73</div><div class="line">74</div><div class="line">75</div></pre></td><td class="code"><pre><div class="line"><span class="comment"># Hexo Configuration  </span></div><div class="line"><span class="comment">## Docs: https://hexo.io/docs/configuration.html</span></div><div class="line"><span class="comment">## Source: https://github.com/hexojs/hexo/</span></div><div class="line"></div><div class="line"><span class="comment"># Site 站点信息设置</span></div><div class="line"><span class="attr">title:</span>  <span class="comment">#站名</span></div><div class="line"><span class="attr">subtitle:</span>  <span class="comment">#副标题</span></div><div class="line"><span class="attr">description:</span> <span class="comment">#站描述</span></div><div class="line"><span class="attr">author:</span>  <span class="comment">#作者</span></div><div class="line"><span class="attr">language:</span> zh-CN <span class="comment">#语言</span></div><div class="line"><span class="attr">timezone:</span></div><div class="line"></div><div class="line"><span class="comment"># URL 链接设置</span></div><div class="line"><span class="comment">## If your site is put in a subdirectory, set url as 'http://yoursite.com/child' and root as '/child/'</span></div><div class="line"><span class="attr">url:</span> http://blog.prozin.xyz</div><div class="line"><span class="attr">root:</span> /</div><div class="line"><span class="attr">permalink:</span> :year/:month/:day/:title/</div><div class="line"><span class="attr">permalink_defaults:</span></div><div class="line"></div><div class="line"><span class="comment"># Directory 文件目录</span></div><div class="line"><span class="attr">source_dir:</span> source</div><div class="line"><span class="attr">public_dir:</span> public</div><div class="line"><span class="attr">tag_dir:</span> tags</div><div class="line"><span class="attr">archive_dir:</span> archives</div><div class="line"><span class="attr">category_dir:</span> categories</div><div class="line"><span class="attr">code_dir:</span> downloads/code</div><div class="line"><span class="attr">i18n_dir:</span> :lang</div><div class="line"><span class="attr">skip_render:</span></div><div class="line"></div><div class="line"><span class="comment"># Writing 文章</span></div><div class="line"><span class="attr">new_post_name:</span> :title.md <span class="comment"># File name of new posts</span></div><div class="line"><span class="attr">default_layout:</span> post</div><div class="line"><span class="attr">titlecase:</span> <span class="literal">false</span> <span class="comment"># Transform title into titlecase</span></div><div class="line"><span class="attr">external_link:</span> <span class="literal">true</span> <span class="comment"># Open external links in new tab</span></div><div class="line"><span class="attr">filename_case:</span> <span class="number">0</span></div><div class="line"><span class="attr">render_drafts:</span> <span class="literal">false</span></div><div class="line"><span class="attr">post_asset_folder:</span> <span class="literal">false</span></div><div class="line"><span class="attr">relative_link:</span> <span class="literal">false</span></div><div class="line"><span class="attr">future:</span> <span class="literal">true</span></div><div class="line"><span class="attr">highlight:</span></div><div class="line"><span class="attr">  enable:</span> <span class="literal">true</span></div><div class="line"><span class="attr">  line_number:</span> <span class="literal">true</span></div><div class="line"><span class="attr">  auto_detect:</span> <span class="literal">true</span></div><div class="line"><span class="attr">  tab_replace:</span></div><div class="line"></div><div class="line"><span class="comment"># Category &amp; Tag</span></div><div class="line"><span class="attr">default_category:</span> uncategorized</div><div class="line"><span class="attr">category_map:</span></div><div class="line"><span class="attr">tag_map:</span></div><div class="line"></div><div class="line"><span class="comment"># Date / Time format 日期</span></div><div class="line"><span class="comment">## Hexo uses Moment.js to parse and display date</span></div><div class="line"><span class="comment">## You can customize the date format as defined in</span></div><div class="line"><span class="comment">## http://momentjs.com/docs/#/displaying/format/</span></div><div class="line"><span class="attr">date_format:</span> YYYY-MM-DD</div><div class="line"><span class="attr">time_format:</span> HH:mm:ss</div><div class="line"></div><div class="line"><span class="comment"># Pagination 分页</span></div><div class="line"><span class="comment">## Set per_page to 0 to disable pagination</span></div><div class="line"><span class="attr">per_page:</span> <span class="number">20</span></div><div class="line"><span class="attr">pagination_dir:</span> page</div><div class="line"></div><div class="line"><span class="comment"># Extensions 扩展</span></div><div class="line"><span class="comment">## Plugins: https://hexo.io/plugins/</span></div><div class="line"><span class="comment">## Themes: https://hexo.io/themes/</span></div><div class="line"><span class="attr">theme:</span> landscape <span class="comment">#默认主题</span></div><div class="line"></div><div class="line"><span class="comment"># Deployment 这里设置了Git获</span></div><div class="line"><span class="comment">#这里一定要注意不要写错了，否则部署到Github上会出问题</span></div><div class="line"><span class="comment">## Docs: https://hexo.io/docs/deployment.html</span></div><div class="line"><span class="attr">deploy:</span></div><div class="line"><span class="attr">  type:</span> git</div><div class="line"><span class="attr">  repo:</span> 仓库地址</div><div class="line"><span class="attr">  branch:</span> master</div><div class="line"><span class="attr">  message:</span> <span class="string">'站点更新:<span class="template-variable">&#123;&#123;now("YYYY-MM-DD HH/mm/ss")&#125;&#125;</span>'</span></div></pre></td></tr></table></figure>
<h4 id="以上部分有个坑需要注意：——-“-”后面都有一个空格。"><a href="#以上部分有个坑需要注意：——-“-”后面都有一个空格。" class="headerlink" title="以上部分有个坑需要注意：——   “:”后面都有一个空格。"></a>以上部分有个坑需要注意：——   “:”后面都有一个空格。</h4><p>保存后退出，执行生成网页命令，然后部署命令，部署成功后就可以访问了。</p>
<h3 id="4-更改主题"><a href="#4-更改主题" class="headerlink" title="4.更改主题"></a>4.更改主题</h3><p>默认的主题虽然也不错，但是一般大家还有更好的选择：NexT</p>
<p>①. 切换到hexo目录下，执行拉去NexT主题项目的命令。<br>②. 好吧具体的流程在这里：<a href="http://theme-next.iissnan.com/getting-started.html" target="_blank" rel="external">NexT的配置使用</a></p>
<h3 id="5-博客"><a href="#5-博客" class="headerlink" title="5.博客"></a>5.博客</h3><p>①. 命令：</p>
<figure class="highlight actionscript"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">hexo <span class="keyword">new</span> <span class="string">"标题"</span></div></pre></td></tr></table></figure>
<p>后会在sourse/ _posts文件夹下生 .md文件，然后就可以编辑内容了</p>
<pre><code>title: Hello World
date: 2015-07-30 07:56:29 #发表日期，一般不改动
categories: hexo #文章文类
tags: [hexo,github] #文章标签，多于一项时用这种格式
---
正文，使用Markdown语法书写
</code></pre><p>②. 发布</p>
<figure class="highlight verilog"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">hexo clean</div><div class="line">hexo <span class="keyword">generate</span></div><div class="line">hexo deploy</div></pre></td></tr></table></figure>
<p><strong>以下信息表示部署成功即可访问：[info] Deploy done: git
</strong></p>
<h3 id="6-访问"><a href="#6-访问" class="headerlink" title="6.访问"></a>6.访问</h3><p>以我的地址为例：<a href="https://paradoxie(替换用户名).github.io/" target="_blank" rel="external">https://paradoxie(替换用户名).github.io/</a></p>
]]></content>
    </entry>
    
  
  
</search>
